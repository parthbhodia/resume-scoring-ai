# Resunova — context for Claude sessions

You're working on **Resunova** (resunova.io), a résumé scoring + tailoring web app. This file is the single source of truth for project state. **Read it at the start of every session, and update it after every commit you land.** The "Recent changes" log at the bottom is how future sessions inherit the architectural decisions you made.

## How to use this file

1. **At the start of a session** — read this top-to-bottom. The "Honesty pipeline" and "Current download path" sections in particular have shifted twice in the last week.
2. **After every commit you land** — append a one-paragraph entry to the "Recent changes" log at the bottom with the commit hash, what changed, and *why*. Don't recap every detail of the commit message — that's already in `git log`. Capture the *architectural decision* or *invariant* that future sessions need to know.
3. **When you change architecture** — also update the relevant section above (component map, pipelines, etc.) so the top of the file stays current.
4. **When you add a new safety net (validator, regex, test class)** — add it to the "Validators + invariants" section.

The point of this file is to keep `git log`-able history out of your context window. Future you should be able to read this once and skip the last 50 commits.

---

## Quick orientation

- **Frontend**: Next.js 16-ish (see `web/AGENTS.md` — heads-up that the framework version has breaking changes vs training data). React, TypeScript, Zustand for state. Lives in `web/`.
- **Backend**: Starlette + uvicorn, Python 3.11+. Lives in `resume_gui/` (modular packages + thin `app.py`) + shared modules in `linkedin_agent/`. Onboarding map: [`resume_gui/README.md`](resume_gui/README.md).
- **LLMs**: Grok-4 (xAI) is the default for everything heavy. Gemini 2.5 Flash/Pro as fallback. No Anthropic API path in production.
- **PDF generation**: TWO active pipelines — see "Current download path" below.
- **Tests**: pytest for backend (`resume_gui/tests/` + `linkedin_agent/tests/`). Frontend has no unit tests yet — the dimension tests at `resume_gui/tests/test_analyze_dimensions.py` are the closest thing to integration tests. Run with `.venv/bin/python -m pytest`.

## Local dev

```bash
# Backend (auto-reload)
.venv/bin/uvicorn resume_gui.app:app --host 0.0.0.0 --port 8765 --reload \
  --reload-dir resume_gui --reload-dir linkedin_agent

# Frontend (Next dev server)
cd web && npm run dev       # → :3000

# Auth bypass for local Analyze testing
# Set in web/.env.local:
NEXT_PUBLIC_DEV_BYPASS_AUTH=true
NEXT_PUBLIC_API_URL=http://localhost:8765
```

## The two main user flows

1. **Analyze** (the priority flow as of late May 2026) — upload PDF → backend extracts structured data via Grok-4 vision → runs comprehensive analysis prompt → returns scores + bulletAnalysis + topIssues + a synthesized clean text. Frontend renders the preview from the synthesized text. User can download the preview as a WYSIWYG PDF via Chromium.

2. **Tailor** (older flow, ResumeBuilder.tsx) — paste a JD → backend extracts structured data → LLM tailors bullets to match JD → renders to LaTeX via Jinja → pdflatex → PDF. This is the path that still uses the Harshibar template.

---

## Architecture: the analyze pipeline

```
POST /api/analyze-upload  (PDF binary)
  │
  ├── extract_upload_markdown        # MarkItDown → text
  ├── _stitch_wrapped_bullets        # rejoin display-line wrapped bullets
  │
  └── _llm_extract(text, pdf_bytes):
        ├── _llm_extract_pdf_vision(pdf_bytes)
        │     ├── _render_pdf_pages_to_b64_pngs  # PyMuPDF → PNG
        │     ├── Grok-4 vision call             # PNG → structured JSON
        │     └── _vision_raw_to_resume_doc      # JSON → ResumeDocModel
        │
        ├── (fallback) _llm_extract_with_manifest(text)  # reasoning model on text
        │
        ├── _normalize_structured_experience / _education / _skills / _contact
        ├── (text path only) _split_collapsed_education_entries
        └── doc.section_order = infer_section_order_from_profile(...)

  └── _synthesize_text_from_resume_doc(doc)    # doc → clean text for preview
  └── _analyze_resume_comprehensive(clean_text, jd)  # → bulletAnalysis, topIssues, scores
        ├── _llm_json_call(prompt, model_override=_analysis_model())  # grok-4 by default
        ├── _validate_analysis_against_resume(raw, text)              # evidence validator
        ├── _strip_non_issue_ats_warnings(raw)                        # always-on
        └── _normalize_analysis(raw)                                  # calibration v2 + rewrite filter
        # struct → prompt hints only; no inject_deterministic_insights on LLM path

return {
  overallScore, categoryScores, summary, topStrengths, topIssues,
  atsWarnings, keywordAnalysis, bulletAnalysis, finalRecommendations,
  extractedText (= synthesized clean text),
  resumeHeader, structuredResume, bulletMap,
}
```

The **vision path is primary for PDF uploads.** The text-based reasoning extract is a fallback for Word docs, vision API failures, or when `DISABLE_VISION_EXTRACT=1`.

### Honesty pipeline (the safety net I built layer-by-layer over the last week)

The LLM is regularly dishonest. It tags bullets as needing quantification when the bullet has plenty of numbers, emits "improved" rewrites identical to the original, lists "No tables detected" as a *warning* even though that's a good fact, fabricates topIssues that contradict what's in the résumé. The honesty layer catches all of this:

| Stage | What it does | Where |
|---|---|---|
| `_filter_bullet_rewrites` | Drops `improvedBullet` if (a) drops numerals/proper nouns from original, (b) shrinks >20% in words, (c) claims `quantification` but adds no new numerals, (d) byte-equal to original after normalize. Also strips lying `quantification` tag from `issues[]`. | `resume_gui/app.py` |
| `_validate_analysis_against_resume` | Counts numerals + strong-verb-led bullets in actual résumé text. Drops topIssues / atsWarnings / bullet issue tags / finalRecommendations whose claim contradicts evidence. Floors quantification/achievementQuality/languageQuality scores to 55 when evidence supports it. | `resume_gui/app.py` |
| `_strip_non_issue_ats_warnings` | Always-on. Drops atsWarnings phrased as non-issues ("No tables detected", "Standard headings", "ATS-friendly layout"). | `resume_gui/app.py` |
| `_normalize_analysis` calibration v2 | Replaces old `weak_penalty + issue_penalty + floor_penalty` stack with `mean(categoryScores) − soft_penalty`. Blends LLM overall with calibrated mean. Rejects LLM overall when divergence > 20. Floors at 20. When validator flagged ≥2 adjustments, ignores LLM overall entirely (it's been proven untrustworthy). | `resume_gui/app.py` |

Tests at `resume_gui/tests/test_analyze_dimensions.py` defend all of these — 58 cases, runs in <1s, **must stay green**.

---

## Current download path

**Single WYSIWYG pipeline** for Analyze, Tailor-to-job, and Template Builder: preview HTML → `useHtmlPdfExport` → POST `/api/export-pdf-html` → Playwright Chromium → PDF. Preview === download.

Hook: `web/hooks/useHtmlPdfExport.ts`. Wired in `AnnotatedResumePanel`, `TailorPreviewPane`, and `TemplateBuilderClient`.

**Preview styling** (font preset + accent swatch) lives in `AnnotatedResumePanel` — client-side CSS vars only; no LaTeX template picker.

**Tailor gap fixes** patch synthesized plain text in the browser and rescore via `/api/analyze` — no `/api/apply-suggestions` / pdflatex on the JD tailor path.

**Template Builder** is `/template-builder/` (replaces legacy `ResumeTemplateStudio` / `?flow=template`).

**LaTeX cleanup candidates** (backend still has dead paths used only if something calls `generate-stream` / `apply-suggestions`):
- `/api/generate-stream`, `/api/apply-suggestions`, `/api/analyze-export-pdf`
- `resume_gui/renderers/latex_renderer.py`, `resume_gui/templates/latex/`, pdflatex in Docker

---

## Component map

### Backend (`resume_gui/`)

- **Entry** — `app.py` (~80 lines): Starlette factory, CORS, test re-exports. Route table from `routes/__init__.py`.
- **Analyze pipeline** — `analysis/comprehensive.py` (LLM prompt + `_analyze_resume_comprehensive`), `analysis/rewrite_validators.py`, `analysis/evidence_validator.py`, `analysis/normalize.py`.
- **Extract pipeline** — `extract/vision.py`, `extract/pipeline.py` (`_llm_extract`, `_finalize_structured_doc`), `extract/synthesize.py`, `extract/structured_doc.py`.
- **Routes** — `routes/analyze.py`, `routes/suggest.py`, `routes/generate.py`, `routes/export.py`, etc. See `resume_gui/README.md`.
- **LLM** — `llm/client.py` (`_llm_json_call`, model tiers via env vars).
- **Validators / honesty** — under `analysis/` (same invariants as before).

### Frontend (`web/components/`)

- `AnalyzeResume.tsx` — top-level Analyze view. Owns the sidebar (TOP FIXES + COMPLETED categorization with actionability gate from `d4f2641`).
- `AnalyzePreviewPane.tsx` — wraps the rendered preview on the right.
- `AnnotatedResumePanel.tsx` — actually contains the Download PDF button. Uses `useHtmlPdfExport` since `af79efc`.
- `AnalyzeLiveResumeBody.tsx` — renders the extractedText into HTML blocks. Section heading detection lives in `looksLikeSectionHeading` (tightened in `b807bfb` to reject lines with digits / `%` / parens).
- `ResumeBuilder.tsx` — the tailor flow. Big file (~3800 lines). Has its own LaTeX-render + HTML-render buttons.

### Hooks (`web/hooks/`)

- `useHtmlPdfExport.ts` — DOM → HTML → POST `/api/export-pdf-html` → download. The path forward.
- `useAnalyzeExport.ts` — legacy LaTeX export. Still imported but the Analyze button no longer calls it.

### Lib (`web/lib/`)

- `resumeLayout.ts` — canonical preview/PDF spacing: presets, CSS vars, TB inline styles, Analyze `paragraphBlockStyle` / bullet stylesheet. Template Builder + Analyze + Tailor should import here (not duplicate in components).
- `analysisCategoryMatch.ts` — frontend mirror of the category bucketing logic. `guessIssueCategory` is the regex pile that maps an issue's text to one of the 8 categoryScores keys. Must stay in sync with backend prompts.
- `resumeFileName.ts` — `ownerSlugFromProfile` derives filename stem from the candidate name.
- `keywordDelta.ts` — `extractKeywords` + `computeKeywordDelta` for the inline +chips/–chips diff UI on each fix card.

---

## LLM model selection

Three model "tiers" controlled by env vars:

| Tier | Default | Use case | Env override |
|---|---|---|---|
| Vision extract | `grok-4` | Reading PDF pages as images | `VISION_EXTRACT_MODEL` |
| Text reasoning extract | `grok-4-fast-reasoning` | Fallback when vision unavailable | `GROK_REASONING_MODEL` |
| Analysis prompt | `grok-4` | Main `_analyze_resume_comprehensive` LLM call | `ANALYSIS_MODEL` |
| Everything else | `grok-4-fast-non-reasoning` | Gap-fix suggestions, JD extract, etc. | `GROK_MODEL` |

`DISABLE_VISION_EXTRACT=1` forces the text path.

Gemini fallback chain kicks in when Grok hits quota or errors. Slugs: `GEMINI_REASONING_MODEL` (default `gemini-2.5-pro`), `GEMINI_FLASH_MODEL` (default `gemini-2.5-flash-lite`).

---

## Validators + invariants (don't break these)

1. **No-op rewrites must not be surfaced.** `_filter_bullet_rewrites` drops `improvedBullet` byte-equal to original.
2. **`quantification` tag implies the rewrite adds a numeral.** Tag is stripped if no rewrite anywhere adds new digits.
3. **Rewrites must preserve numerals and proper-nouns from original.** Validator rejects on drop.
4. **Issues that contradict the résumé must be dropped.** "Missing metrics" on a résumé with ≥6 numerals → dropped. "Duty-only" on a résumé with ≥60% strong-verb-led bullets → dropped.
5. **ATS warnings phrased as good facts ("No tables detected", "Standard headings") are not warnings.** Always stripped.
6. **A category in TOP FIXES must have actionable content** (≥1 flagged bullet OR ≥1 related topIssue). Otherwise it moves to COMPLETED.
7. **Score has a floor of 20.** Anything that parsed and produced category scores deserves at least 20.
8. **The Analyze "Download PDF" matches the preview.** Goes through Chromium, not LaTeX.
9. **The backend owns bullet category bucketing.** Each `bulletAnalysis` item carries `primaryCategory` + `issueCategories` (validated by `_normalize_bullet_categories` against `_CATEGORY_SCORE_KEYS`). The frontend trusts them verbatim; it does NOT re-derive categories with regex when they're present. A bullet's `primaryCategory` must be a category it can offer a rewrite for, and "quantification" never appears in either field without a numeral-adding rewrite. The `guessIssueCategory`/`inferBaseCategory` heuristics survive only as a fallback for legacy restored-history payloads.
10. **Display uses `issueCategories`; rewrites use `primaryCategory` only.** `bulletBelongsToCategory()` (frontend) checks `issueCategories` for filtering, preview highlights, sidebar badge counts, and `categoryHasActionableContent`. `getRewriteForCategory()` still keys off `primaryCategory` so the UI never surfaces a rewrite for the wrong fix target. Preview banner is red only when `flaggedCount > 0` for the active category.
11. **Strong-verb detection distinguishes verb+object from participle+duty-noun.** `_bullet_leads_with_strong_ownership_verb()` in `analysis/constants.py` rejects leads like `Automated testing…` (participle + activity noun) while keeping `Automated CI/CD pipelines…`. Used for résumé-wide share, evidence validator per-bullet checks, and achievement rewrite filtering.
12. **`achievementQuality` rewrites must change the opening and fix the lead.** `_validate_achievement_rewrite` in `rewrite_validators.py` drops `improvedBullet` / `categoryRewrites.achievementQuality` when the first token is unchanged or the rewrite still opens with a participial duty-style phrase.
13. **Pronoun advice must match the text.** `_sanitize_pronoun_claims_in_text_fields` strips unsupported "remove personal pronoun" clauses from `sectionFeedback` (per-section scope) and from `topIssues` / `categoryRationales` / `finalRecommendations` when the full résumé has no pronouns. Always-on (not gated on numerals/strong-verb thresholds).
14. **Buzzword detection is universal-filler only.** `_BUZZWORDS` in `comprehensive.py` flags clichés with zero concrete meaning in any field (`team player`, `results-driven`, …). Domain-ambiguous terms (`framework`, `scalable`, `efficient`, `reliable`, `leverage`, `proven`, …) must never be flagged — they are legitimate vocabulary in some disciplines.
15. **Deterministic topIssues are regex-fallback only.** `inject_deterministic_insights` runs when the comprehensive LLM call fails — not on normal analyze. `_recruiter_checks` still feeds `structural_signals` into the LLM prompt. Frontend hides `source: "deterministic"` topIssues so saved runs stay clean.

The dimension tests in `resume_gui/tests/test_analyze_dimensions.py` plus `test_experience_tenure.py` defend invariants 1–13; `test_deterministic_insights.py` defends 14–15 (buzzword list + inject helper for fallback). Full algorithm notes: [`docs/ANALYSIS_ALGORITHM.md`](docs/ANALYSIS_ALGORITHM.md).

```bash
.venv/bin/python -m pytest resume_gui/tests/test_analyze_dimensions.py resume_gui/tests/test_experience_tenure.py -v
# (no .venv in this container — `python3 -m pytest …` after the dep-install noted in the changelog)
```

---

## Known gaps (intentionally not yet fixed)

- **No frontend unit tests.** When validators ship false negatives that only surface as UI bugs (the "empty AI Suggestion popup" and "TOP FIXES with no fixes" of recent memory), there's no test that catches it. Would need Vitest + React Testing Library setup. Tracked in conversation as Tier 2.
- **Backend contract tests** that assert the *shape* the frontend depends on (e.g. "if `improvedBullet === ''` and `categoryRewrites === {}`, this bullet should be considered non-actionable"). Cheaper than frontend tests, would have caught both UI bugs above. Tracked as Tier 1.
- **The synthesizer and the LaTeX template duplicate display logic.** Section ordering, year-extraction, tech-stack rendering — they're implemented twice. As long as Tailor uses LaTeX, this stays. Migration plan: move Tailor's output to HTML → Chromium too, then delete the LaTeX subsystem.
- **`web/.env.local` overrides** are project-developer only; `NEXT_PUBLIC_DEV_BYPASS_AUTH` ships in source but is gated.

---

## Recent changes (running log — newest first; **append after every commit**)

- **🚀 PROD: free-tier daily scan cap raised 3 → 10 (api `resunova-api` staging `161f3e4` + main `de5266b`, 2026-07-19)** — Owner: bump the free-tier daily scan limit. Root: `resume_gui/services/scan_limits.py` `_daily_scan_limit()` env-var `FREE_SCAN_DAILY_LIMIT` **defaulted to 3** on `resunova-api` (the deploy repo), but **`resunova-web` `lib/scanLimits.ts` already ships `FREE_DAILY_SCAN_LIMIT = 10`** and drives ALL quota copy dynamically from the API `limit` field (`ScanUsageCard`, `AnalyzeResume`, `scanGreeting` — greeting test even asserts "all 10 free scans") — so the UI already promised 10 while the server enforced 3. Bumped the code default (+ ValueError fallback) 3→10 in a throwaway worktree off `origin/staging` (shared checkout was on `ip-history-work` w/ a parallel agent's uncommitted jobs work — not touched); 13/13 `test_scan_limits.py` pass; FF-pushed `b94117d..161f3e4` to staging. Also synced this monorepo's stale copy (its default was 5, ships nowhere) → 10 for consistency. **Contract preserved:** `limit` flows straight through to `GET /api/scan-limit-status` (`limit`/`remaining=max(0,limit-used)`) + the 429 `{code:"daily_scan_limit_reached", limit}` body — value change only, no shape change. **No migration:** the cap is computed live from today's `resume_analyses` count, NOT stored per-user, so it raises for all existing free-tier users immediately. **Anon per-IP cap left at 1** (`ANON_SCAN_DAILY_LIMIT`, default 1) — deliberate: signing in is what unlocks the 10/day tier, matching the "sign in for 10 scans/day" funnel copy. **Code promoted to prod:** cherry-picked `161f3e4` onto `origin/main` → `de5266b` (clean auto-merge, 13/13 tests pass; `f90d0b3..de5266b`), Railway auto-deploys `main`. Done via throwaway worktree off `origin/main`; shared `ip-history-work` checkout untouched. **⚠️ STILL VERIFY (owner):** the Railway **env var** — if `FREE_SCAN_DAILY_LIMIT` is explicitly SET to `3` in the Railway env (staging and/or prod `serene-playfulness`→`production`), it OVERRIDES the new code default and the cap stays 3 until you unset it or set it to `10`. Setting `FREE_SCAN_DAILY_LIMIT=10` there is also the instant no-deploy fix. **ROLLBACK: revert `de5266b` on main / `161f3e4` on staging; and unset/lower the env var.**

- **🚀 PROD: Jobs feed "Outside your filters" fallback section on empty results (web `resunova-web` main via PR [#45](https://github.com/parthbhodia/resunova-web/pull/45), 2026-06-26)** — A search/filter combo returning nothing was a dead-end ("No openings in this window"). Now whenever the visible list is empty, the feed fetches a **relaxed set** (same search + résumé ranking, but NO facet filters and NO posting-age cap) and renders it in a separate **"Outside your filters"** section beneath the empty state. **Implementation (`JobsFeed.tsx`):** (1) extracted the inline job card into a reusable `renderJobCard(job, reasonChip?)` (the `pagedJobs.map` body, ~160 lines) so the fallback reuses the exact card; (2) `loadFallback` hits `/api/jobs/feed` with `role` + `analysis_id` + `title_any` ONLY (drops `work_model_any`/`seniority_any`/`min/max_years`/`industry`/`employment_type` + `max_age_days`); the section renders WITHOUT the client country/state/score filters (it IS the out-of-filters bucket), capped at `FALLBACK_LIMIT=12` + a "widen a filter" hint; (3) each fallback card gets a muted reason chip via `outsideReason()` (🌍 Outside US / 📅 Older posting / 🏢 Other work model); (4) fires whenever `ready && pagedJobs.length===0` (the default US + past-month ARE filters, so NOT gated on an explicit filter), and `loadFallback` returns `[]` when even the relaxed set is empty so the section just doesn't render; the `loadFallback` cache key is `search|role|analysis` (filter-independent) so changing only a facet doesn't refetch. tsc clean; Pages deploy success; `www.resunova.io`→200. **NOT browser-QA'd** (authed feed + CORS-blocked local harness — same as every JobsFeed change). **INVARIANT: the fallback section deliberately ignores ALL filters (server facets + client country/state/score) — it's the "what's out there" escape hatch; don't re-apply filters to it. It reuses `renderJobCard`, so card changes stay in one place.** **ROLLBACK: web revert PR #45.**

- **🚀 PROD: role families for education/finance/trades/design + title-search synonyms ("physio"→"physical therap") — api `resunova-api` main `c7880bf` + prod-data backfill, 2026-06-26** — Finished the role-family sweep (after physio/legal/BA) and fixed the **search box** (separate code path from the classifier). **Trigger:** a screenshot showed searching **"physio" → 0 jobs**. Root cause: the keyword box does a literal `title ILIKE`, and this US corpus stores **"physical therapy"** (only 2 titles contain "physio") — the classifier fix only affects the role-scoped FEED, NOT the search box. **Verified the deploy IS live first** (Railway prod URL = `https://resume-scoring-ai-production.up.railway.app`, service `resume-scoring-ai`; got the deployed commit via `railway status --json` → confirmed running). **Classifier (`role_family.py`):** added the missing common terms to **education** (tutor, lecturer, teaching assistant, academic advisor, school/guidance counselor, educator, faculty, substitute/assistant/school principal, …), **finance** (accountant [was only "accounting"!], banker, underwriter, actuary, loan officer, mortgage, bookkeeper, credit analyst, treasury, tax accountant/associate/analyst/manager/preparer, wealth management, financial advisor/planner, teller, payroll, AP/AR, investment analyst, equity research), **skilled_trades** (welder, machinist, carpenter, mechanic, pipefitter, millwright, mason, forklift, boilermaker, ironworker, sheet metal, fabricator, drywall, roofer, glazier, lineman, painter, maintenance/automotive/diesel technician, heavy equipment operator), **design** (illustrator, art director, creative director, animator, motion graphics). **Search (`jobs.py`):** new `_TITLE_SYNONYMS` + `_expand_title_terms` (mirrors the existing `_LOCATION_ALIASES`/`_expand_location` pattern) maps physio↔physical therap, occupational/speech therapy, slp→speech-language pathologist, rn→registered nurse; keeps the typed term, dedupes; wired into the count + ranked-feed title search. **Ambiguous tokens EXCLUDED (would pollute):** bare `principal` (seniority), `technician` (pharmacy/IT/lab), `counselor` (camp/genetic), `advisor` (customer), `portfolio manager` (clinical/product/project — caught by the regression: "Clinical Portfolio Manager" was flipping healthcare→finance, so dropped), and abbrevs `pt`/`ot`/`ba`/`pm` in the synonym map. **Verified on 33,829 corpus titles vs deployed: 707 re-homed (394 finance, 231 trades, 55 design, 23 education + a few defensible cross-family), 0 pollution; 6 ambiguous-token guards pass.** +4 regression tests; 68 passing. **Backfill (30d window, classifier-driven — re-ran the deployed classifier on each general/null title, not hand-SQL, to stay exactly consistent): 793 re-tagged (finance 405, trades 308, design 64, education 16).** Scoped to 30d because that's the feed window; the 134k all-time general/null backlog is mostly stale. **INVARIANT: `_TITLE_SYNONYMS` (search) and `_FAMILY_SIGNALS` (scoping) are the two levers — search box = literal title match (needs synonyms for corpus vocab), feed-with-no-search = role-family scoped. A term can be broken in one but not the other.** **ROLLBACK: api `git push origin 0fec997:main --force-with-lease`.** **NOT live-QA'd end-to-end** — the authenticated feed/search needs a signed-in session (no token; CORS blocks the local harness); verified via deploy-commit check + offline regression + unit tests. The 5 verticals (physio/allied-health, legal, BA, + edu/fin/trades/design) now cover the audited list.

- **🚀 PROD: business-analyst variants routed (BI→data, business systems/process→operations) — api `resunova-api` main `0fec997` + prod-data backfill, 2026-06-26** — Audit of the BA vertical (next after lawyer/physio). **"Business Analyst" already worked** (→`operations`, where 149/232 = 64% of BA-titled jobs already live), so BA was NOT broken like physio/lawyer — but several variants fell to `general`: **Business Intelligence / BI Analyst, Business Systems Analyst, Business Process Analyst**. **Fix (`0fec997`):** `business intelligence` + `bi analyst` → **data** (BI is data-domain); `business systems analyst` + `business process analyst` → **operations** (with `business analyst`). **Disambiguation was the hard part — used FULL phrases only:** a bare `intelligence analyst` would have captured **security / threat / all-source intel** roles, and a bare `systems analyst` spans **IT** ("Computer Systems Analyst") and **finance** ("Financial Systems Analyst") — all intentionally left out. **Verified on 33,829 corpus titles: 35 re-homed (18→data all BI, 16→ops all business systems/process, 1 BI-engineer software→data), 0 pollution; 12 guard cases pass** (security/IT/finance analysts stay put). +2 regression tests. **Backfill:** 90 `general`/`null` → data, 57 → operations. **NOTE: BA inherently scatters across families** (the same résumé-ranking single-family-pool model means a "Business Analyst" user scoped to `operations` won't see BA jobs tagged data/software/finance — that's the model's design, not a bug; a cross-family "analyst" meta-scope would be a larger change). **ROLLBACK: api `git push origin a1cd0fd:main --force-with-lease`.**

- **🚀 PROD: lawyer / solicitor / etc. now classify `legal` (same feed-scoping fix) — api `resunova-api` main `a1cd0fd` + prod-data backfill, 2026-06-26** — Follow-up to the allied-health fix below, same root cause in `classify_role_family`. The `legal` family matched `attorney`/`counsel`/`paralegal`/`litigation` but NOT **"Lawyer"** (the term a job seeker is most likely to enter) nor Commonwealth equivalents (**Solicitor, Barrister**) or **Law Clerk / Judicial Clerk / Prosecutor / Public Defender** — all fell to `general` → feed broadened off the legal pool. **Fix (`a1cd0fd`):** added those whole-token signals. **Intentionally excluded `advocate`** (customer/patient/brand advocate) and `compliance` (finance/ops) — too ambiguous (would pollute legal). **Verified on 33,829 real corpus titles vs the deployed classifier: 13 flip INTO legal (all genuine), 0 flip out, 0 pollution.** +2 regression tests. **Backfill:** re-tagged **450** active `general`/`null` postings with legal titles (lawyer/attorney/paralegal/solicitor/law-clerk/…) → `legal`; legal pool now 1,372 active / 902 in 30 days. Corpus is US-centric (15 "lawyer" vs 58 "attorney"; barrister 0, solicitor 2) — the value is scoping a **"Lawyer"-preference USER** to the legal pool, not the few lawyer-*titled* jobs. **ROLLBACK: api `git push origin 104d658:main --force-with-lease`.** **GENERAL PATTERN (worth a sweep): `classify_role_family` families are keyword lists that miss common synonyms — any vertical whose seekers use a term not in `_FAMILY_SIGNALS` gets dumped to `general` + a broadened feed. Candidates to audit next: education ("tutor", "lecturer", "TA"), finance ("banker", "underwriter", "actuary"), trades ("welder", "machinist", "carpenter", "mechanic"), design ("graphic designer", "illustrator").**

- **🚀 PROD: physiotherapy / allied-health roles now classify `healthcare` (so the ranked feed surfaces them) — api `resunova-api` main `104d658` + prod-data backfill, 2026-06-26** — A friend "couldn't find physiotherapy jobs." Root-caused, NOT a corpus gap: prod has ~248 physical-therapy + 574 "therapist" + 2,199 healthcare-ish postings in 30 days. The bug is `classify_role_family` (`resume_gui/tailor/requirement_match/role_family.py`): the healthcare family's only therapy token was the bare word `therapist` (`\btherapist\b`), so a user's saved-role / résumé term of **"Physiotherapist", "Physiotherapy", "Physical Therapy", "Occupational Therapy", "Speech Language Pathologist", "Athletic Trainer", "PT"** all classified **`general`**. The feed (`api_jobs_feed`) scopes the candidate POOL by `feed_family` (saved-pref family, else résumé family); `general` → `_query_postings(None)` broadens to the **600 most-recent across ALL families**, where ~248 PT jobs (0.4% of the 56.8k active corpus) are invisible. (The postings themselves already classify `healthcare` via their JD body — only the USER side never scoped to the pool.) **Fix (`104d658`):** added whole-token allied-health signals to the `healthcare` family (physio/physical/occupational/speech/respiratory therapy + athletic trainer, rehabilitation, kinesiology, chiropractic, podiatry, audiology, dietitian, phlebotomist, sonographer, home health, dental hygienist, dpt, …). Word-boundary matching needs whole tokens (a `therap` stem fails the trailing `\b`), so each surface form is listed. **Bare "PT" intentionally excluded** (collides with "part-time"). **Verified on 33,829 real distinct corpus titles: 237 flip INTO healthcare (all genuinely clinical — podiatrist, SLP, home-health aide, PTA), 0 flip OUT, 0 non-clinical pollution.** +2 regression tests (`test_requirement_match.py`); 59 pass. **Prod-data backfill (immediate discoverability, not just future scans):** re-tagged **1,675** active postings that were `general`/`null` but whose title clearly matches an allied-health signal → `healthcare` (PT-in-healthcare 239→248; healthcare-30d pool now 5,863). **INVARIANT: classifier is symmetric (postings classified at scan via JD body+title; user role/preference at feed time) — allied-health role terms must land `healthcare` on BOTH sides; never add a bare-`pt` signal (part-time collision).** **SECONDARY GAPS (noted, not fixed):** (1) **search-term mismatch** — this US corpus indexes "physical therapy/therapist"; the literal word "physiotherapy" appears in only **2** titles, so a search for "physiotherapy" returns ~nothing while "physical therapy" returns 246 → a synonym/expansion layer on the title search would help. (2) **600-cap within healthcare** — a healthcare user sees only the 600 most-recent of 5,863 (≈26 PT in that window, floated up by résumé ranking); a dedicated `allied_health`/`therapy` sub-family would give a tighter pool but needs re-tagging existing `healthcare` postings. **Railway auto-deploys main (prod URL is a Railway secret — not directly probed).** **ROLLBACK: api `git push origin c981258:main --force-with-lease`; the backfill is additive (re-tag clinical→general only if needed).**

- **🚀 PROD: feed cards drop the MATCH badge + "X/Y matched" requirements (web `resunova-web` main via PR [#44](https://github.com/parthbhodia/resunova-web/pull/44), 2026-06-26)** — Completes the lazy on-click-scoring pivot (api `e8b8379` / web PR #43): with the score now computed when you OPEN a job, the precomputed feed-card `MATCH` badge + terracotta `matchedCount/totalRequirements` requirements bar were stale/inconsistent (some cards scored, most not — `matchScore == null` showed a "See match ?" placeholder). Removed BOTH score-badge blocks (the scored one + the `null` "See match" placeholder) and the requirements progress bar from each card in `JobsFeed.tsx`, plus the now-unused `colors`/`reqPct` locals. Cards are now a clean listing: `CompanyLogo` (size 44) + title + company·location·date + salary/work-model/seniority tags + actions; the match score lives only on the detail (`JobDetail.tsx`, where the live cloud extraction + scoring runs). `tsc` clean; Pages deploy success; `www.resunova.io`→200. **NOT browser-QA'd** (feed needs an authed session + CORS blocks the local harness — same constraint as every JobsFeed change). **INVARIANT: feed cards carry NO match score/requirements — scoring is on-open only; don't reintroduce a precomputed card badge while the lazy model stands.** **ROLLBACK: web revert PR #44.**

- **🚀 PROD: recent-scans picker shows the NAME, not the contact header (web `resunova-web` main via PR [#31](https://github.com/parthbhodia/resunova-web/pull/31), 2026-06-25); + extraction 7B benchmark → reverted to 14B** — Owner: the Update-résumé picker "doesn't look good" — each row showed `resume_analyses.label`, which is usually the résumé's FULL contact header (`NAME | phone | email | linkedin…`), truncated mid-word into a wall of text. Fix: `label = rawLabel.split("|")[0].trim()` → just the name. `tsc` clean, shipped. **Extraction model benchmark (ops, not git):** tried `qwen2.5:7b-instruct` at `EXTRACT_CONCURRENCY=2` on the 16 GB M2 Pro — clean (`extracted=8 failures=0`, 0 timeouts) but prod throughput was **~500/hr vs 14B's ~440/hr (only ~15% faster)** because extraction is **I/O-bound on the per-sweep Supabase round-trips, not the GPU** (7B finishes ~3× faster per call then waits on the DB). Not worth 7B's weaker `requirement_concepts` (they drive match scoring) → **reverted to `qwen2.5:14b-instruct`** (`linkedin_agent/.env` `EXTRACT_LLM_MODEL`; worker `~/.resunova/prod-extract-worker.sh` `EXTRACT_CONCURRENCY=1`, restarted; 14B confirmed loaded). **The real throughput lever is batching the DB writeback (bulk-update the sweep's rows in one call) — deferred, code change to `scan_worker`/`run_scan`.** **RESOLVED (single-extractor architecture): (1) `full_scan.py` is now DISCOVERY-ONLY by default (`extract_limit` from `FULL_SCAN_EXTRACT_LIMIT`, default 0) — api `43f8cca` main + `3350803` staging; the 6am cron worktree pulls main, so the scan no longer extracts and only the worker touches the GPU. (2) `linkedin_agent/.env` `EXTRACT_CONCURRENCY` set back to 1 (belt). Worker verified healthy on 14B post-revert: `extracted=4 failures=0`, 0 timeouts.** **INVARIANT: exactly ONE extractor on this single-GPU box — the continuous `scan_worker` (conc=1). The daily scan discovers only; never re-add inline extraction to the scan while the worker runs (set `FULL_SCAN_EXTRACT_LIMIT` only if the worker is stopped).**

- **🚀 PROD: Workday provider ingests ALL recent roles, not just new-grad (api `resunova-api` main `4262d4f`, 2026-06-25)** — Root-caused the "big employers land ~7 jobs each" mystery: the `WorkdayProvider` (`resume_gui/jobs/providers.py`) was scoped to **early-career/new-grad only** (a leftover from the student-audience era) via a new-grad `searchText`, a new-grad TITLE regex, and a senior-role drop — while Greenhouse/Lever ingest every role and the feed serves all seniority. So RTX landed 3 of 4,234, Walmart 8 of 2,000, J&J 4 of 1,857, AbbVie 0; the existing 156 Workday tenants sat at ~7 jobs each. **Fix:** paginate the CXS list **newest-first with empty `searchText`** (all roles/levels), stop once a posting is older than the feed window (`postedOn` is relative + newest-first → everything after the first out-of-window hit is older). New constants: `_WORKDAY_MAX_KEPT=300`/board, `_WORKDAY_MAX_AGE_DAYS=30`, `_WORKDAY_TIMEOUT=30` (was 20), `_WORKDAY_DETAIL_CONCURRENCY=6`; new `_workday_postedon_age_days()` parser. Dropped `_WORKDAY_NEWGRAD_TERMS/_TITLE_RE/_SENIOR_RE/_MAX_PAGES_PER_TERM`. **Live RTX smoke (cap 25): 25 returned, 10 senior/mid (old code: 0), JD+recent dates present.** 51 provider tests pass (rewrote the Workday fetch test for all-roles + recency-stop). **INVARIANT: each Workday posting needs a separate detail GET for the JD — the recency-window + `_WORKDAY_MAX_KEPT` cap bound per-board detail volume; tune down if the daily scan slows or Workday rate-limits.** Applies on the next daily scan → ~171 Workday tenants jump from ~7 to up to 300 recent all-level roles each (tens of thousands more postings) → **raises feed-pending volume, making a faster extraction path (cloud burst / 7B) more pressing.** **ROLLBACK: `git push origin fa60542:main --force-with-lease`.**

- **🚀 PROD RELEASE: "Update résumé" → pick from recent scans / rank against any past scan (api `resunova-api` main+staging `fa60542`/`7b22e0f`; web `resunova-web` main via PR [#30](https://github.com/parthbhodia/resunova-web/pull/30), 2026-06-25; `/ui-ux-pro-max`)** — Owner: after scanning/optimizing, let the user switch which résumé the feed ranks against without re-uploading — show recent scans (top 5, scrollable) under "Update résumé". **Backend (`api_jobs_feed`):** new optional `?analysis_id=` — when set + owned by the user, ranks against THAT `resume_analyses` row instead of the latest (falls back to latest when absent/not-found/not-owned). The feed `store_key` already embeds `analysis_row.id`, so per-scan caching is automatic. **Candidate POOL is unchanged (still scoped by saved role preference) — only the RANKING changes** (the honest framing). 27 jobs/feed tests pass; FF to main + clean cherry-pick to staging. **Frontend (`JobsFeed.tsx`):** the Update résumé dialog now renders `fetchAnalyses(5)` (lazy on open) as a scrollable list above the upload — each row = score badge (`scanScoreStyle`) + label + `scanTimeAgo`; the active one shows "✓ Now ranking" (= `rankAnalysisId || recentScans[0].id`, since the backend default ranks against the latest = `recentScans[0]`). Picking one sets `rankAnalysisId` (persisted in `localStorage[JOBS_RANK_ANALYSIS_KEY]`), which is threaded into `loadFeed` + `loadMoreFromServer` params (`analysis_id`) and `browseFeedCacheKey` (4th arg `a:<id>`) + their deps → the `[loadFeed]` effect refetches ranked against the chosen scan and caches it separately (SWR-correct on remount). A new upload (`startResumeRanking`) clears the override (fresh scan = new latest). Upload path unchanged. `tsc` clean. **INVARIANT: switching the ranking résumé must clear on new upload; `rankAnalysisId` belongs in the feed cache key (4th browseFeedCacheKey arg) AND every feed-fetch param site (loadFeed + loadMoreFromServer) or paging/remount desyncs which scan ranks.** **NOT browser-QA'd** (feed needs an authed session + scan history). **ROLLBACK: web revert PR #30; api `git push origin 140d931:main --force-with-lease`.**

- **🚀 PROD RELEASE: Optimize/Boost preview now highlights accepted changes (web `resunova-web` main via PR [#29](https://github.com/parthbhodia/resunova-web/pull/29), 2026-06-24)** — Owner: the Step-3 "Tailored résumé" preview didn't highlight applied suggestions, unlike Analyze + the Tailor flow. `BoostPanel.tsx` Step3 rendered `AnalyzeLiveResumeBody` with `highlightsEnabled={false}` + no highlight props. Fix: new `appliedHighlights` = text of every ACCEPTED suggestion (`drafts[i]?.trim() || s.suggested`); passed as `tailorAppliedHighlights` with `highlightsEnabled={appliedHighlights.length > 0}` → the changed bullets get the same green "applied" highlight (matched via `resumeLineMatchesSuggestionOriginal`, rendered line == suggested text → "applied" kind in `tailorHighlightKind`). **PDF export unaffected** — `exportPdf(..., {highlightsEnabled:false})` adds `az-highlights-off` which CSS-zeroes the bullet tints/bars (`lib/resumeLayout.ts`), same mechanism Analyze/Tailor use. `tsc` clean. **INVARIANT: highlighting requires `highlightsEnabled && presentationOnly` (+ `bulletAnalysis.length===0` for the flat-text path); the green "applied" vs purple "gap" kind comes from `tailorAppliedHighlights` vs `tailorGapFixHighlights` string-match — pass the APPLIED bullet text, not the original.** **ROLLBACK: web revert PR #29.**

- **🚀 PROD RELEASE: "More from {company}" panel no longer mislabels other-company jobs (web `resunova-web` main via PR [#28](https://github.com/parthbhodia/resunova-web/pull/28), 2026-06-24)** — Owner: the right-column "MORE FROM RUBY LABS" panel showed Truelogic/Stripe/Alpaca/etc. Root cause in `components/MoreMatchesPanel.tsx`: it built `items = [...sameCompany, ...rest].slice(0, MAX_CARDS)` (padding the same-company list with other-company feed matches to fill 6 cards) but set the heading to `More from {company}` whenever `sameCompany.length >= 1` — so 1 real same-company job + 5 padded others all sat under "MORE FROM {company}". Fix: split into **two correctly-labeled sections** — `More from {company}` (same-company only, `sameCompany.slice(0,6)`) and `More matches` (the rest, `rest.slice(0,6)`); shared `renderCard`. `tsc` clean. **INVARIANT: the "More from {company}" group contains ONLY that company's postings — never pad it with other-company matches; padding goes under the separate "More matches" heading.** **ROLLBACK: web revert PR #28.**

- **🚀 PROD RELEASE: Jobs feed stops reloading on every tab switch + Google-style JD collapse (web `resunova-web` main via PR [#26](https://github.com/parthbhodia/resunova-web/pull/26), merge `c9b416b`, 2026-06-24; `/ui-ux-pro-max`)** — Owner: "why does the job keep loading every time I switch tabs?" + showed Google Jobs and said "take notes." **(1) Reload bug — `JobsFeed.tsx` stale-while-revalidate:** `loadFeed` only served the module-level `feedCache` when `<FEED_TTL_MS` (5min) fresh AND key-matched; otherwise it `setState({status:"loading"})` (skeletons) + refetched the SLOW ranked feed — so every app-tab return after the TTL (or a cold remount) skeletoned. Fix: (a) `state` now SEEDS from `feedCache?.data` in the `useState` initializer → instant feed on remount, no skeleton flash; (b) `loadFeed` serves the cache immediately even when STALE and only revalidates in the BACKGROUND (`const cached = feedCache?.key===cacheKey`; skeleton path reserved for a true cold load with nothing to show); (c) a transient **401 during background revalidation** (Supabase token-refresh race) no longer bounces a user-with-a-shown-feed to `signin` (gated `&& !cached`); (d) a failed background revalidation keeps the stale feed (`if (cached) return` in catch). **(2) JD detail → Google pattern (`JobDetail.tsx`):** "About this role" is **collapsed by default** (`maxHeight:300` + bottom fade gradient) with a **"Show full description ⌄"** toggle (only when `jdText.length>800`) — replaces last turn's nested scroll-pane (skill flags nested scroll regions); + a prominent **"Apply ↗"** CTA (`job.url`) near the top of the detail header (Google places apply up top). Full-width 2-col fill from PR #25 retained. `tsc` clean. **NOT browser-QA'd** (detail needs an authed session + job data; CORS blocks the local harness). **INVARIANT: feed remount must serve `feedCache` (SWR) — never skeleton when cache exists; JD is collapsed-by-default, expand via state not nested scroll.** **ROLLBACK: web revert PR #26.** **Follow-up shipped — PR [#27](https://github.com/parthbhodia/resunova-web/pull/27) (merge, prod):** widened the feed rail `flex:0 0 420px` → `clamp(360px, 40%, 560px)` (Google list proportion) so more of the list is in view; detail fills the remaining ~60% with the 2-col fill RETAINED (grid breakpoint lowered `minmax 400→360` so JD|match stays side-by-side at the narrower detail, stacks only on smaller screens). Kept 2-col rather than pure single-column on purpose — a single-col detail at ~60% would float the match-panel widget in whitespace (re-creating the wasted-space complaint). ROLLBACK: web revert PR #27.

- **🚀 PROD RELEASE: Jobs detail view fills full width — 2-col grid + scrollable JD (web `resunova-web` main via PR [#25](https://github.com/parthbhodia/resunova-web/pull/25), merge `49173b1`, 2026-06-24)** — Owner (screenshot): big wasted band on the right of the job-detail master-detail + the JD card too tall. Two causes: (1) **`HomePageClient` JobsView** capped the master-detail at `maxWidth:1500` centered → empty space on wide monitors → removed the cap (detail column `flex 1 1 0` now fills all width left of the fixed 420px feed rail). (2) **`JobDetail` body** was flex-wrap `[JD flex 1 1 480] + [fixed 340px panel]` → the panel WRAPPED below the JD at in-between widths, leaving the right half empty → now a width-filling **2-column grid `repeat(auto-fit, minmax(400px,1fr))`** (left = header + JD, right = match/prep/insider); collapses to 1 col < ~820px / mobile (non-embedded). (3) **JD scroll pane**: capped to `maxHeight:calc(100dvh-320px)` + `overflowY:auto` in embedded (desktop) view so a long JD no longer dominates; `alignItems:start` keeps the right column pinned at top. `embedded` now threaded into `JobBody`. `tsc` clean. **NOT browser-QA'd** (detail needs an authed session + job data; CORS blocks the local harness — same as every JobsFeed/JobDetail change). **INVARIANT: the JobsView detail is full-bleed (no maxWidth) — feed rail stays 420px, detail flexes; the 2-up split is `auto-fit minmax(400,1fr)` so it self-collapses, don't reintroduce a fixed-width right panel.** **ROLLBACK: web revert PR #25.**

- **🚀 PROD RELEASE: Tailor/Optimize fixes #1 (honest scoring) + #2 (suggest-then-accept) — api `resunova-api` main `af36b9f`→`7f33b69`; web `resunova-web` main via PR [#23](https://github.com/parthbhodia/resunova-web/pull/23), 2026-06-24** — Owner: "Optimize my résumé" flow had a match score jumping ~41%→~80% on ONE keyword + suggestions auto-applied ("N/N APPLIED") instead of suggest-then-accept. **Fix #1 (backend `7f33b69`, two compounding causes):** (a) **empty-bucket inflation** — `calculate_weighted_jd_score` (the `/api/analyze` JD-Tailor scorer) used `_safe_ratio`→`1.0` for buckets the JD never populated, so absent concept buckets each contributed their FULL weight for free (up to ~70pts before a single requirement matched). The weighted OVERALL now **excludes empty concept buckets + renormalizes** over present weights (same approach `_feed_match_score` already uses; the BoostPanel scorer was already honest). Scoped to the overall sum ONLY — per-bucket display values, `JdMatchBreakdown` counts, `_safe_ratio`/`_bucket_score` untouched. Verified: 1/10 keyword coverage **82→35**; 8/10 stays 85. (b) **single-append multi-flip swing** — new `_damp_boost_after_score(before, after, n)` caps the gain above before-score at **8pts per accepted suggestion** (skipped keywords earn none), applied in `api_jobs_boost` (`len(changes)`) + `api_jobs_boost_score` (`len(accepted)`). +7 regression tests; `test_weighted_scorer_exclusion`+`test_jobs_boost`+`test_job_providers`+`test_requirement_match` = **83 passed**. **INVARIANT: the per-suggestion damp lives ONLY in the boost routes** (they have before/after+count); the Analyze/Tailor paste-a-JD path gets the renormalization (honest baseline) but not the cap (it rescores whole-résumé, no per-suggestion granularity). **NOTE: this recalibrates the `/api/analyze` JD-match score for NEW scans — previously-inflated scores drop; saved `resume_analyses` snapshots keep old numbers.** **Fix #2 (web PR #23):** `BoostPanel.tsx` Step3 defaulted `accepted` all-true (`setAccepted(suggestions.map(()=>true))` line 482) → flipped to all-false + per-card `checked` fallback `?? true`→`?? false`. Every downstream value already keys off `accepted[]`: counter→`0/N`, `headlineScore`→`beforeScore` at 0, `appliedText` revert loop→original text (CLIENT-SIDE; no API change — `/api/jobs/boost/score` only powers partial-selection scores). "Accept all"/"Clear" + skipped-keyword honesty line unchanged. `tsc` clean. **Process:** both via clean worktrees off `origin/main` (shared api checkout on `ip-history-work`, web on `feature/tb-import-resume`); api FF-pushed to main (staging cherry-pick ABORTED — staging `jobs.py` diverged +418 w/ 3 conflicts, left to parallel agent; `weighted_scorer.py` applied clean there but not pushed); web PR #23 `--admin` merged. **NOT browser-QA'd** (Tailor needs auth+JD; verify the score recalibration + the 0/N review-first state on deploy). **Fix #3 (Tailor preview ⇄ Analyze editing/reorder parity) → 🚀 PROD via PR [#24](https://github.com/parthbhodia/resunova-web/pull/24) (merge `048eccd`, Pages deploy success, `www.resunova.io`→200; owner said "merge it to prod" — shipped WITHOUT live QA per owner go).** Implemented: `TailorPreviewPane` ADDS `fieldsEditable` + edit-handler props while KEEPING `presentationOnly` (the parity contract `AnalyzePreviewPane` uses — the prior attempt flipped `presentationOnly` and collapsed the pane to 460px + killed gap-fix highlights, since it's overloaded for width + highlight gating). `ResumeBuilder` gains `tailorFieldOverrides` (path-keyed `exp.0.bullets.2`) + popup handlers. New lib `applyFieldOverridesToStructured` bakes path-based neutral-bullet edits onto the **structured doc** (the authoritative rescore input — index-based `patchStructuredWithOverrides` doesn't cover path edits) at ALL 4 `structured_resume` send sites (initial analyze / main rescore / generate-stream / suggest-gap-fix), so inline edits move the score with NO preview↔score desync. Section reorder/rename come free (`sectionEditable = fieldsEditable || !presentationOnly`). `tsc` clean; helper unit-checked (resolve/no-op/clone/bounds). **NOT browser-QA'd (shipped on owner go) — verify on prod: neutral-bullet edit persists+scores, reorder keeps gap-fix highlights, layout stays full-width (NOT 460px), PDF strips edit chrome. The risk if wrong is layout collapse or edit↔score desync — watch for those.** **ROLLBACK: api `git push origin af36b9f:main --force-with-lease`; web revert PR #23 (#2) + PR #24 (#3).**

- **🛠️ OPS: prod jobs EXTRACTION un-stalled — serialized the local Ollama worker (`~/.resunova/prod-extract-worker.sh`, 2026-06-24, not in git)** — Extraction was dead: every sweep `extracted=1 failures=9` (9/10 timed out) + a trivial Ollama call hung 90s. Root cause: the worker fired `EXTRACT_CONCURRENCY=4` parallel 14B requests at a single-GPU Ollama on a **16 GB** M2 Pro (10 GB model, Ollama `--parallel 2`) → memory thrash → timeouts. **Fix: serialized to `EXTRACT_CONCURRENCY=1 BATCH=4`** (one request in flight = one Ollama slot = fits RAM), unloaded the jammed model, `launchctl kickstart -k`. Kept **qwen2.5:14b** per owner. Verified: single realistic request ~44s/HTTP200, **0 timeouts** since restart, DB ground truth **~150 rows enriched/20min (~450/hr)** — exceeds the ~111/hr daily inflow, so the freshest jobs (newest-first claim order) stay current and the backlog (137k, mostly old) chips down. **2-in-flight is NOT safe at 16 GB** — leave at 1 unless the box gets more RAM. **Sources are greenhouse/ashby/lever/workday/jibe only — NO Oracle/Taleo adapter exists** (the 6 new adapters SmartRecruiters/Teamtailor/JazzHR/Jobvite/BambooHR/Rippling still yield 0 rows — weak seeds); Workday tenants work (45→1,052).

- **🚀 PROD RELEASE: Jobs feed footer shows the REAL total ("342 of 9,338 jobs"), not "342+" (api `resunova-api` main `f638d23`→`af36b9f`; web `resunova-web` main via PR [#22](https://github.com/parthbhodia/resunova-web/pull/22), 2026-06-24)** — Owner: "342 of 600" was misleading (600 = page cap, 342 = after the client US-only filter) and the follow-up "342+ jobs" copy fix (PR #21) still hid the real total. **Now the footer shows how many jobs actually match the user's feed scope.** **Backend (`routes/jobs.py` `api_jobs_feed`, cherry-picked onto main, FF — main unprotected):** new in-handler `_count_postings(used_family)` returns an **exact, `head=True`** count (no rows) of `is_active AND requirement_concepts IS NOT NULL` postings matching the **SAME** filters the candidate query used (role `used_family` / `location` ilike / `work_model` / `posted_at` recency, **minus** order+range) → added to the payload as **`totalMatching`**. Best-effort: any count failure → `None` and the feed still renders. Uses the codebase's existing count idiom (same as `/api/jobs/public-count`). **Frontend (`components/JobsFeed.tsx`):** `FeedReady.totalMatching?: number` captured in all 3 feed setters + preserved across load-more (page-invariant); shared `feedCountLabel` → **`"342 of 9,338 jobs"`** when `total > visible` (paging cap and/or active client filter hiding some), **falling back to the old `"342+"/"342"`** when the backend sends no total (older API / count failed). Both the compact header + the toolbar footer use it. **INVARIANT — `totalMatching` is the SERVER feed-scope count; it does NOT know the client-side country/US filter (no country column exists — see [[`lib/jobsLocation.ts`]]), so `visible ≤ loaded ≤ total` always holds and "of TOTAL" reads as standard "showing X of Y results".** **Validated on prod data** (`eiumlptnsmowvkxucprl`, project secret-free SQL): 30-day feed-scope counts are meaningful per family — software ≈ 9,338, sales ≈ 6,039, general(broaden fallback) ≈ 9,749, smallest design ≈ 623. `tsc --noEmit` clean (Node 20); `feedCountLabel` branches unit-checked (of/+/singular/fallbacks). **NOT browser-QA'd** (feed needs an authed session + the deployed API; CORS blocks the local harness — same constraint as every JobsFeed change). **Web Pages prod deploy in-progress at hand-off** (PR #22 merged `--admin`; PR #21 pattern succeeded ~1m17s — confirm `gh run` success). **API Railway prod deploy NOT directly verifiable** (prod URL is a Railway secret) — owner confirm project `serene-playfulness` env `production`. **staging NOT updated on web** — `origin/staging` `JobsFeed.tsx` has diverged hugely (+393/−73, the parallel agent's WIP, 7 cherry-pick conflicts); left untouched per the no-disturb rule (the api `totalMatching` field IS on staging via `aa989e7`). **ROLLBACK (additive/code-only): api `git push origin f638d23:main --force-with-lease`; web revert PR #22.**

- **🚀 PROD RELEASE: ranked Jobs feed scoped by saved role preference, not last résumé (api `resunova-api` main `75b3c6a`→`14d4766`, staging PR [#7](https://github.com/parthbhodia/resunova-api/pull/7), 2026-06-24)** — Owner: "which jobs to show → driven by the user's saved job preference, not the résumé's inferred role." Before, `api_jobs_feed` scoped the candidate set via `classify_role_family(resume_text, …)` — coupling *which* jobs surface to whatever résumé was last scanned (the root of the earlier "change role doesn't work in the ranked feed" problem). **Fix (`routes/jobs.py`, ~line 1003/925):** compute `feed_family` from the user's `user_profiles.profile.roles` (`classify_role_family(" ".join(profile_roles), profile_roles[0])`) when set; fall back to the résumé's family only when no preference or it classifies "general". `_query_postings(feed_family)` replaces `_query_postings(resume_family)`. **Ranking is unchanged — the résumé still scores/sorts; the preference only decides the candidate POOL.** Non-breaking: no preference → identical behavior; the <20-candidate broaden-to-recent fallback is intact; the feed cache key already includes `profile_roles` so changing the preference invalidates the cache. **Validated on the prod corpus (54.5k active+enriched):** every family the common prefs map to (software 11.7k, sales 8.1k, marketing 3.1k, product 1054, design 764, data 544, … smallest psychology 71) clears the 20-floor → preference scoping never empties the feed. **INVARIANT: preferred LOCATION stays a soft ranking signal + the country toggle — NOT a hard filter (would empty the feed); role is the scoping lever.** app imports; 93 jobs/feed/role_family tests pass; cherry-picked clean onto main + re-tested. **NOT live-verified** (feed needs an authed session; staging Supabase is DOWN — only prod can exercise it). **Railway prod deploy not directly verifiable** (prod URL is a Railway secret) — owner confirm project `serene-playfulness` env `production`. **Still pending from the same ask: the "update résumé" popup** (re-scan from the ranked feed) — not built yet. **ROLLBACK: `git push origin 75b3c6a:main --force-with-lease`.**

- **🚀 PROD RELEASE: removed the redundant `AppTopBar` (the "second navbar") — web `resunova-web` staging `bf264bb` + main via PR [#20](https://github.com/parthbhodia/resunova-web/pull/20), 2026-06-24** — Owner: "why am I seeing 2 navbars? can we use only 1, on the sidebar?" The "second navbar" was **`components/app-shell/AppTopBar.tsx`** — a global `h-14` header rendered inside `SidebarInset` on **every** view. On desktop its left half is empty (`hidden md:block`) and the right half is just an account-avatar dropdown that **duplicates** the sidebar footer's user menu (`AppSidebarUser`); on mobile it carried the logo + auth zone. Pure redundancy on desktop where the sidebar is already the single nav. **Fix = delete `AppTopBar` entirely** (owner chose "remove entirely" over desktop-only-hide): dropped the import + `<AppTopBar/>` block from `AppShell.tsx` and removed the component file (−126 lines). Nothing orphaned — `SignInDialogProvider`/`useSignInDialog` stay wired for the rest of the shell, no `app-top-bar` CSS to clean, no other importers. **Where auth lives now:** desktop = sidebar footer (`AppSidebarUser` menu signed-in; "Sign in — free" item for anon); mobile = bottom-nav "More" sheet (account / sign out / sign in) + locked tabs still funnel to `SignInDialog`. **Process (diverged branches):** `main` (prod) was **23 commits AHEAD** of `staging` (recent jobs-feed work), staging only 1 ahead — NOT a fast-forward promotion. Edited on a throwaway worktree off `origin/staging` → pushed `staging` (`bf264bb`); then **cherry-picked the same commit** onto a worktree off `origin/main` → PR #20 → `gh pr merge --admin` (`1e831c3` / merge `09ffd74`). **Verified:** `tsc --noEmit` clean on both; staging + prod GitHub Pages deploys both **success**; `www.resunova.io` → 200, 0 `app-top-bar` in served output. Shared `resunova-web` checkout untouched on `feature/tb-import-resume`; both worktrees + temp branches cleaned up. **NOT browser-QA'd by me** (env is Node 16; project needs 20 — verified via tsc + deploy success); owner to hard-refresh and confirm account access from the sidebar. **ROLLBACK (pure deletion, additive-safe): `git revert 09ffd74` on main + revert `bf264bb` on staging — or restore `AppTopBar.tsx` + its two `AppShell.tsx` lines.** **Heads-up for the diverged worktrees** (`feature/tb-import-resume`, `builder-ai-suite`, `two-column-template`): they still carry `AppTopBar.tsx` + its AppShell wiring; drop it when they next rebase on staging/main so the second navbar doesn't come back.

- **🚀 PROD RELEASE: Jobs feed Country filter (default US) — web `resunova-web` PR [#15](https://github.com/parthbhodia/resunova-web/pull/15) → main, 2026-06-24** — Owner report: after the onboarding wizard the feed "shows all the jobs" incl. Philippines/Korea/China, no country filter ("states hora sirf"), US not prioritized. **Root cause:** `job_postings` has **NO country column** — only free-text `location` ("Austin, TX", "Manila, Philippines"). The only location filter was a US-**state** multi-select, client-side, applied **only when states were picked**; the wizard's location step is **optional** → finishing it without a location left the feed unfiltered across all countries. **Fix (frontend only):** new **`web/lib/jobsLocation.ts`** (testable) — moved `US_STATES`/`locationMatchesState` here + added `isUSLocation()` (US state name/abbr, US tokens incl. "Remote - US"/"US-CA-…"/"US > AZ" + major US metros w/o a state: San Francisco/Chicago/Boston/…) and `isClearlyInternational()` (foreign countries + regions [Europe/LATAM] + major foreign cities [London/Paris/São Paulo/Bengaluru/…], whole-word, **gated by a positive-US override** so "Vancouver, WA"/"Cambridge, MA USA" stay US). `JobsFeed.tsx` gets a **Country toggle** (🇺🇸 US only [default] / 🌍 All countries) that drops `isClearlyInternational` postings; session state, clears with "Clear all". **Tested on PROD data** (staging Supabase was DOWN — "not accepting connections"; prod `eiumlptnsmowvkxucprl` now carries the **54,500**-posting corpus — prod is NO LONGER empty): sampled real `location` strings, **144 vitest cases** over them, `tsc` clean, full suite 166. Corpus-wide: **31.8% (17,315) clearly international → now hidden**; 52.5% US + 15.7% ambiguous = 68% kept; 364 kept-despite-foreign-token (US override worked). **INVARIANT: no country column exists — country/US scope is derived from free-text `location` in `lib/jobsLocation.ts`; keep its lists in sync if the corpus shifts.** Residual: bare "Manchester" (35) intentionally stays (collides w/ Manchester NH). **NOT visually UI-tested** (feed needs an authed session) — web main Pages deploy in-progress at hand-off. **Known follow-up (optional, prob. unneeded):** backend US-bias of the candidate fetch — moot since 68% of the corpus is US/ambiguous. Default is US-only; flip to US-first-sort if owner prefers showing all. **ROLLBACK: web revert PR #15.**

- **🚀 PROD RELEASE: Interview Prep hardening → main on BOTH repos (api `resunova-api` main `d22c589`→`75b3c6a`; web `resunova-web` main `d1e2311`→`3394993` via PR [#14](https://github.com/parthbhodia/resunova-web/pull/14), 2026-06-24)** — Owner-authorized "full send." Promoted exactly this session's Interview-Prep work via **surgical cherry-pick, NOT a full staging→main merge** — both repos' `main` had diverged/prod-ahead commits (api main ahead by 4, web main ahead by 10) a blind merge would've dragged into prod (incl. a "two-phase analyze" twin already on api main). **API (2 commits, cherry-picked onto a worktree off `origin/main`, FF push — main unprotected):** (1) **skills sanitizer** `_looks_like_prose_not_skill` in `extract/doc_normalize.py` — drops bullet fragments leaking into the skills list (impact-metric / trailing sentence punctuation / >6-word no-comma phrase), complements `_looks_like_accomplishment_bullet` which only caught verb-led ≥8-word bullets; runs in `_normalize_structured_skills` (both extract paths). (2) **interview-prep**: per-section regenerate via `only_section` (refreshes one card; **never mutates the persisted kit** — guarded the wipe + both persist blocks with `not only_section`); fixed `load_prep_questions` **section_id key mismatch** (stored long `resume_questions` vs section_map's short `resume` → saved questions never loaded back, silently breaking "resume your last kit" + the POST cached path); gated the POST cached-return behind a new `prefer_cache` flag (default OFF) so a fresh run never returns a stale kit; **prep-history endpoints** `GET /api/interview-prep/sessions` (list) + `GET /api/interview-prep/session/{id}` (owner-scoped load) backed by `list_prep_sessions` + `get_prep_session_by_id`. 27 interview/structured-doc tests pass; 76 routes import. **Web (1 commit, cherry-picked onto a worktree off `origin/main`, PR #14 + `gh pr merge --admin` — main protected):** resume-from-history pickers on Step 1 (`ResumeHistoryPicker` = "use a saved resume" from analyzed history via `fetchAnalyses`/`fetchAnalysisById`; `PrepHistoryPicker` = "your prep history" → opens a saved kit on the dashboard); replaced the raw parsed-resume dump (leaked parse artifacts) in `ResumeUploadCard` with a compact success state + detected category; dashboard `GeneratingState` skeleton + cycling status + a11y `role=status` toast + Regenerate-All busy state; scoped per-section regenerate; `lib/supabase` `fetchPrepSessions`/`fetchPrepSessionById` + `mapPrepSession` helper; store one-shot `historySessionId`. tsc clean. **NO DB migration** — reads existing `interview_prep_sessions` columns; degrades gracefully (empty history) if `question_count` is absent on prod. **INVARIANTS:** per-section regenerate is a live-view refresh that never writes the DB; `prefer_cache` is opt-in and the client never sends it (mount always generates fresh; "resume last kit" uses GET /latest, now fixed). **Process landmines handled:** web shared checkout is on the parallel agent's `feature/tb-import-resume` with their uncommitted work across ~8 files (BoostPanel/CoverLetter/JobDetail/JobsFeed/docs) — extracted ONLY my 7 files via a `git diff` patch → throwaway worktree off `origin/staging` (`git apply --3way`; resolved 2 keep-both conflicts in `PrepDashboardView` where staging's `CodingBankSection` real-question-bank feature collided with my `GeneratingState`), never touched their branch; my edits still linger uncommitted in that checkout (harmless dupes of what shipped). API local checkout was **stale** (behind origin/staging) → committed on a throwaway work branch, cherry-picked onto fresh worktrees off origin/staging then origin/main. **VERIFICATION GAP:** NOT browser-QA'd — dedicated `app/interview-prep/*` routes hang in `next dev`; the history/regenerate flows need a signed-in session with saved kits. Web **staging** Pages deploy **succeeded**; web **main** Pages deploy was **in-progress** at hand-off (identical commit → confirm `gh run` success + `www.resunova.io` 200). **API Railway prod deploy NOT directly verifiable** (prod URL is a Railway secret) — owner confirm project `serene-playfulness` env `production`. **ROLLBACK (all additive/code-only): api `git push origin d22c589:main --force-with-lease`; web revert PR #14.** Full session arc: skills sanitizer → resume-from-history picker → hide parsed dump → in-flow feedback → scoped regenerate → `load_prep_questions` fix → prep history.

- **🚀 PROD RELEASE: two-phase analyze-upload (api `resunova-api` main `2b015b2`→`d22c589`; web `resunova-web` main → PR [#13](https://github.com/parthbhodia/resunova-web/pull/13), 2026-06-24)** — Owner-authorized ("ship both to prod now"). Cuts the Jobs progressive-ranking latency: after the onboarding wizard, `POST /api/analyze-upload` blocked ~15s on the comprehensive analysis before `GET /api/jobs/feed` could rank. **The feed/detail/boost only read `result.extractedText` + `result.structuredResume`** — the comprehensive analysis (bulletAnalysis/categoryScores/topIssues) is consumed ONLY by the Analyze view. So analyze-upload is now **two-phase, behind an opt-in `defer_analysis` form flag** (`resume_gui/routes/analyze.py`): **phase 1** extracts→synthesizes→gates→persists a *profile-only* `resume_analyses` row and returns in ~1–2s with `analysisPending:true`/`analysisStatus:"profile_ready"`; **phase 2** a Starlette `BackgroundTask` recomputes the full analysis + JD match and overwrites the SAME row via new `_update_analysis_result(id,user_id,…)` (scoped by id+user_id; in `services/persistence.py`). **INVARIANT — the flag is default-OFF and the Analyze view must NEVER send it** (it needs the full synchronous result; `AnalyzeResume.tsx` builds its own FormData without the flag). Only the Jobs flow opts in: `resunova-web` `JobsFeed.tsx` `analyzeResumeUpload()` appends `defer_analysis=1` (commit `35cb5cf`). Flag is forward-compatible (an API without this code ignores the unknown field → full sync). **No DB migration** (readers don't check status, so no status column — the spec's `user_analyses`/status-column idea was unnecessary; live table is `resume_analyses`). **No lazy-finalize** for detail/boost (they only need the phase-1 fields). **Defer gated on an authenticated user** (feed reads by JWT `user_id`); anonymous defer → full sync. **Verified:** +9 tests (route branching + `_update_analysis_result`), full api suite 363 passed (1 pre-existing env fail: `pyspellchecker` dict absent); cherry-pick onto main re-tested green; api main+staging both carry it (`d22c589` / `1d75fa8`). **Web Pages deploy finalizing; `www.resunova.io`→200. Railway api prod deploy NOT directly verifiable (prod URL is a Railway secret — owner confirm project `serene-playfulness` env `production`).** Closes api issue #5. **ROLLBACK (additive/code-only): api `git push origin 2b015b2:main --force-with-lease`; web revert PR #13.**

- **🚀 PROD RELEASE: this session's work → main on BOTH repos (api `resunova-api` main `4cf9027`→`48f55fc`; web `resunova-web` main `eb00105`→`bc99473` via PR #4, 2026-06-17)** — Owner-authorized ("push to prod"). Promoted exactly the 6 commits authored this session, no parallel-agent commits in the diff. **API:** `main` unprotected → FF `git push origin 48f55fc:main` (1 commit: public `/api/jobs/public-count`); Railway prod auto-deploys `main`. **Web:** `main` protected → PR [#4](https://github.com/parthbhodia/resunova-web/pull/4) staging→main, merged `gh pr merge 4 --merge --admin` (5 commits: sign-in top bar + shared modal + first-run wizard `44a5e93`, inline gates → modal `d7e2dc1`, mobile bottom-nav redesign `eff69e2`, signed-out Jobs lean hero `18001db`, signed-out Jobs gated job-board preview `4e31147`). **Verified:** web Pages deploy run **success**; `www.resunova.io` → 200; the landing "Sign up — free" relabel is in the served prod HTML (confirms my bundle is live). **NO DB migrations or new env vars** for any of it (first-run wizard uses existing `user_profiles.profile`; public-count reads existing `job_postings`). **Prod-data caveat (no breakage):** prod `job_postings`=0 rows → prod public-count returns 0 → the signed-out Jobs proof chip falls back to "Live openings updated daily"; the gated hero (search/filters/chips/blurred cards) still renders fully. **API deploy NOT directly verified** — the prod API URL is a Railway/GitHub secret (`RAILWAY_API_URL`) not in the prod bundle's first chunks; owner should confirm the Railway prod deploy (project `serene-playfulness`, env `production`). Endpoint is verified identical on staging (`{"count":16889}` HTTP 200). **ROLLBACK (all changes are additive/code-only): api `git push origin 4cf9027:main --force-with-lease`; web revert PR #4 (or `git push origin eb00105:main` with `--admin`).**

- **Signed-out Jobs page → gated job-board landing + public count endpoint (api `resunova-api` `48f55fc`, web `resunova-web` `18001db`→`4e31147`, all on `staging`)** — Owner: the signed-out Jobs view "has a lot of wording." It stacked 3 value-prop blocks + dead chrome (Recommended/My-Applications tabs, a "Jobs for you" header whose copy said "ranked against your latest analyzed résumé" — which a signed-out visitor doesn't have, a real wrong-context bug, a dead Refresh button, then a sign-in card). **Backend:** new PUBLIC `GET /api/jobs/public-count` (`routes/jobs.py` + registered in `routes/__init__.py` BEFORE the parameterized `/api/jobs/{posting_id}` so it isn't shadowed) — total active+enriched postings in a recent window (default 30d), no auth, no filters, **estimated** count (fast/safe on the 216K table), cached per (window, corpus_version), `{"count": null}` on failure. Verified live on staging: `{"count":16889}` HTTP 200 (existing authed `/api/jobs/count` still 401s). **Web (`18001db`):** when JobsFeed `status==="signin"`, early-return a focused `SignedOutJobsHero` BEFORE the header/Refresh chrome; `JobsTabShell` hides the tabs when not signed in (session check); the wrong-context header copy can no longer render pre-sign-in. **Web (`4e31147`, per "make it bigger + more filters/options"):** grew the hero from a small card into a real **gated job-board preview** — a search bar + Location/Past-month/Work-model filter dropdowns + role category chips + a larger blurred results grid (auto-fill ≤3 cols) with a centered "Sign in to see your matches" unlock card (grid height-capped + bottom fade so the card stays in view on tall mobile). **Invariant:** every control on the signed-out hero is gated — any interaction calls `openSignIn()` (the shared modal); no real data is fetched/shown pre-sign-in (the blurred cards are static, the count is the only live value). Proof chip falls back to "Live openings updated daily" if the count is null. **Verified** desktop + 375px, light + dark via preview (temporary `?signin=1` escape hatch to force the state, since CORS blocks pointing localhost at the staging feed; hatch reverted before commit). `main` untouched on both repos.

- **Mobile bottom nav redesign: 5 slots + icon-tile More sheet (web `resunova-web` `eff69e2` on `staging`)** — Owner feedback: the mobile bottom nav had too much wasted space + too much wording. Root issues found: **6 items** (`analyze/builder/library/jobs/profile` + More — over the ≤5 platform limit), long labels truncating ("Resume Builder" → "Resume Buil…"), a wordy ~8-row text-list "More" sheet, and **Cover Letter + Interview Prep had NO mobile entry point at all** (not tabs, not in More). Changes (2 files): (1) `nav-config.ts` — `MOBILE_TAB_VIEWS` → `analyze · jobs · builder · library` (4 tabs + More = 5); **dropped Profile from the bar** (now reachable via the new top-bar avatar + the More sheet — removes redundancy, frees a slot); new `MOBILE_TAB_LABELS` map for short non-truncating labels (`builder → "Resume"`). (2) `AppBottomNav.tsx` — bigger 22px icons + sentence-case 12px labels (was 9px uppercase that left the 56px bar looking half-empty/cramped); the More sheet rebuilt from a text list into a **2-col icon-tile grid** (Cover letter, Interview prep, Profile, History [+ Analytics for advisors]) with an inline dark-mode toggle + a compact Contact / Report-a-bug / Sign-out row. Anon visitors: gated tiles open the shared sign-in modal (`onSignIn`), Interview Prep stays public; added `useRouter` for the `/interview-prep` dedicated route. **Verified at 375px** (preview `--webpack`, Node 20), light + dark: 5 even tabs no truncation, More-sheet grid + toggle render correctly, zero console errors. **Invariant:** the bottom bar is capped at 5 slots (4 tabs + More); secondary destinations live in the More sheet, not the bar. `main` untouched — promote with the other staging commits after QA.

- **Sign-in fast-follow: remaining inline gates funneled to the shared modal (web `resunova-web` `d7e2dc1` on `staging`)** — Finishes the consolidation below. Retired the last bespoke sign-in surfaces so every gated action opens the one `SignInDialog`: (1) **AnalyzeResume** — deleted the hand-rolled "That was your free scan" overlay (its own backdrop + Google button = a duplicate of the modal) + its `anonGateOpen`/`gateSigningIn` state; the 2nd-anon-scan trigger now calls `openSignIn({reason})`. (2) **ResumeLibrary** + **AdvisorDashboard** — dropped each component's own `signInWithGoogle()`+`oauthBusy` plumbing; their "Sign in with Google" cards call `openSignIn()`. (3) **ResumeBuilder** — the ATS/job-match sign-in prompt opens the modal (`atsOAuthBusy` is now a `const false`; modal owns busy). (4) **`hooks/useSupabaseSignedIn`** — `signIn()` now opens the modal instead of its own OAuth redirect, so **all** Cover Letter + Template Builder "🔒 Sign in to generate" AI gates funnel through the same surface from one change (consumers' `signingIn` stays false; modal owns the busy label). **Net −109 lines** (removed duplicate OAuth handlers). **Verified:** tsc clean; dev server (Node 20, webpack) compiles with zero console errors; confirmed in-browser that the Cover Letter "Sign in to generate" gate opens `SignInDialog` (title "Sign in to Resunova"). **Invariant (now repo-wide):** new sign-in entry points call `useSignInDialog().openSignIn()`, never `signInWithGoogle()` directly — the hook has a fallback to direct OAuth if rendered outside `SignInDialogProvider`. Same throwaway `git worktree` off `origin/staging`. `main` still untouched — promote after live QA.

- **Sign-in consolidated to ONE surface: app-shell top bar + shared modal + first-run wizard (web `resunova-web` `44a5e93` on `staging`)** — Owner ask (via `/ui-ux-pro-max`): sign-in was reinvented in ~14 places (sidebar footer, bottom-nav sheet, locked-nav badges, and bespoke inline cards in Analyze/Jobs/Library/Advisor/CoverLetter/TemplateBuilder), there was no persistent top-right auth zone like EnhanceCV/Kickresume/Jobright, and the only "new-user wizard" was buried inside Profile (a fresh user lands on Analyze and never sees it). **The Jobs wizard already existed and works** (`JobsOnboardingWizard`, fires on feed `needsRole`). **Built (3 new components + 4 edits):** (1) `components/SignInDialog.tsx` — `SignInDialogProvider` + `useSignInDialog().openSignIn({title,reason})`; one branded modal (value props + "Continue with Google"; Google-only so single action). Has a fallback to direct OAuth if rendered outside the provider. (2) `components/app-shell/AppTopBar.tsx` — persistent top-right zone inside `SidebarInset`: anon → "Log in" (ghost) + "Sign up — free" (filled); signed-in → avatar dropdown (Profile/Account/Sign out), which also gives **mobile** signed-in users an account menu they previously lacked. (3) `components/FirstRunWizard.tsx` — Jobright-style 2-step (target-role chips → save to `user_profiles.profile.roles` via `upsertUserProfile`; then "Scan my résumé"/"Browse jobs"). **Conservatively gated:** signed-in + non-UMBC + `user.created_at` within 24h + `!hasUsedAnonScan()` + per-user `localStorage[rn_first_run_v1:<id>]` — so it never nags returning users or covers the anon-scan→sign-in hand-off. (4) **AppShell** split into outer (`AppShell` renders `<SignInDialogProvider>`) + `AppShellBody` (consumes the hook) so the provider wraps the sidebar, top bar, bottom nav AND every view child; `onSignIn` now opens the modal instead of jumping to OAuth — so the sidebar footer "Sign in — free", bottom-nav row, and locked-nav items all funnel to the one modal. Also rewired the Jobs "Sign in to browse jobs" card → modal, gave the **Application Tracker** unauth card a real button (it had NONE — pure dead-end), and relabeled the **Landing** bare lock-icon CTA → "Sign up — free" (consistent funnel). **Invariant:** new sign-in entry points call `useSignInDialog().openSignIn()`, NOT `signInWithGoogle()` directly, so the surface stays consistent. **Verified:** tsc clean; `next dev --webpack` (Node 20) compiled all routes with zero console errors; screenshotted the top bar, the shared modal (light + dark), and both first-run wizard steps (step 2 personalizes by chosen role). **Deferred surfaces (now DONE in `d7e2dc1`, see entry above):** Analyze anon gate, ResumeLibrary + AdvisorDashboard cards, ResumeBuilder ATS prompt, and all CoverLetter/TemplateBuilder AI gates were migrated to `openSignIn()` in the fast-follow. **Process:** built in a throwaway `git worktree` off `origin/staging` (shared checkout is on `feature/tb-import-resume`); dev-previewed via a relative symlink `resume-scoring-ai/_rwsignin → /tmp/rw-staging-signin` + a `rw-signin-audit` entry in `.claude/launch.json` (preview tool requires a cwd inside the project root). **PROD:** `main` untouched — promote staging→main (web protected branch → PR) after live QA of the real Google OAuth round-trip + the first-run wizard on a brand-new account.

- **PROD promotion: redirect fix + feed fix → main (api `resunova-api` main `759befd`→`4cf9027`, web `resunova-web` main `e62fe5f`→`eb00105` via PR #3, 2026-06-17)** — Owner-authorized. **API:** `main` unprotected → FF `git push origin 4cf9027:main` (1 commit = the `user_profiles.profile` jsonb feed fix); Railway prod auto-deploys. **Web:** `main` protected → PR [#3](https://github.com/parthbhodia/resunova-web/pull/3) staging→main (1 commit = the OAuth redirect fix `65ded56`), merged `gh pr merge 3 --merge --admin`; GitHub Pages prod deploy ran (`www.resunova.io` 200). Both staging→main diffs were exactly 1 clean commit each (no parallel-agent commits pending), so promotion was surgical. **State after:** the warm jobs UI (`0cc99cd`) was already in prod from the earlier release; now the redirect fix + feed fix are too. The api feed fix is currently moot on prod (prod `job_postings`=0 rows) but correct for when the corpus lands. **Rollback:** api `git push origin 759befd:main --force-with-lease`; web revert PR #3 (or `git push origin e62fe5f:main` with admin). Prod Supabase untouched (already had `saved_job_filters`; `user_profiles` jsonb schema matches the new api code).

- **Sign-in from Jobs flow bounced to Analyze instead of returning to Jobs — OAuth redirect fix (web `resunova-web` `65ded56` on `staging`)** — Reported: Jobs → "Sign in with Google" → after OAuth the user landed on the default **Analyze** view, losing the Jobs context (and the onboarding wizard). Root cause: `signInWithGoogle` (`lib/anonScan.ts`) set `redirectTo` to just `window.location.origin + BASE_PATH`, dropping the current `?view=jobs`; OAuth returned to bare `/`, where both `HomePageClient`/`AppShell` default `view` to `"analyze"` (`params.get("view") || "analyze"`). **Fix (belt-and-suspenders, allowlist-independent):** `signInWithGoogle` now (a) passes the full `window.location.href` as `redirectTo` AND (b) stashes the destination in `localStorage[POST_LOGIN_DEST_KEY="rn_post_login_dest"]` before OAuth; `AppShell`'s auth effect restores it on mount via `router.replace(dest)` — **one-shot** (atomic getItem+removeItem) and only when the URL has no `view` (i.e. Supabase's redirect allowlist stripped the query back to the origin). Works whether or not the allowlist preserves query params; never hijacks later bare-`/` visits (stash only set right before OAuth, cleared on first consume). Shared helper, so it also returns anon-scan-gate / locked-nav sign-ins to their originating view (anon-analysis localStorage stash still restores independently — no regression). **Not headlessly verifiable** (needs a real Google OAuth round-trip); tsc clean, staging Pages deploy succeeded. Landed via throwaway `git worktree` off `origin/staging` (node_modules symlinked to tsc there) since the shared checkout is on `feature/tb-import-resume` — feature tree untouched. **Correction to the entry below:** prod Supabase **already has `saved_job_filters`** (verified via prod MCP `eiumlptnsmowvkxucprl`), so only the api `4cf9027` feed fix is missing on prod; prod `user_profiles` is the same schema (profile jsonb, no roles/locations cols) and prod `job_postings`=0 rows, so the feed bug won't surface on prod until its corpus lands.

- **Staging jobs feed + saved-filters both 500'd — two schema/code mismatches fixed (api `resunova-api` `4cf9027` on `staging` + staging-DB migration 025)** — Reported live: `GET /api/jobs/feed` and `GET /api/job-filters` 500ing on staging (only on the *authenticated* path; unauth gave a clean 401). Diagnosed from Supabase Postgres logs + `to_regclass`/`information_schema` + reading the deployed `origin/staging` handlers — **without** reusing the user's pasted bearer token. (1) **`/api/job-filters` 500** = the `saved_job_filters` table never existed on staging (migration `resunova-web/db/migrations/025_saved_job_filters.sql` unapplied); the handler returns the raw "relation does not exist" as the 500. **Fix:** applied 025 (table + `(user_id,updated_at desc)` index + RLS owner-all policy) to staging Supabase `uvnncrtulyezsylcxnhw` via `execute_sql` + `notify pgrst,'reload schema'`. (2) **`/api/jobs/feed` 500** = `api_jobs_feed` did `user_profiles.select("roles,locations")`, but staging `user_profiles` has **no such columns** — schema is `user_id, profile jsonb, notify_prefs, updated_at` (migration 003); the web profile form writes the whole `ProfileFormState` into the `profile` jsonb (roles/locations as comma-separated **strings**). The select raised `42703 column user_profiles.roles does not exist` (Postgres logs 05:27:31–33 UTC bracket the user's 05:27:32 request; that error serializes to ~119 bytes = the observed content-length). **Fix:** `select("profile")` then read `prof.get("roles")/.get("locations")` from the jsonb — also restores profile-aware ranking (was silently disabled by the same mismatch). Candidate query ruled out (470ms via `job_postings_browse_recent_idx`). Landed via a throwaway `git worktree` off `origin/staging` → `push HEAD:staging` → Railway (verified healthy 401×24 through the redeploy). **Invariants:** (a) roles/locations live in `user_profiles.profile` jsonb as comma-strings — never as top-level columns; (b) `saved_job_filters` (025) must be applied per-env. **NOT end-to-end verified** with a real auth'd session — user to retest. **PROD CARRYOVER:** prod `main`=`759befd` lacks this `4cf9027` fix and prod Supabase lacks `saved_job_filters` — when prod gets jobs data, the SAME two 500s will hit prod; cherry-pick `4cf9027` + apply 025 to prod before prod jobs go live.

- **🚀 PROD RELEASE: staging → main on BOTH repos (api `resunova-api` main `f94ec60`→`759befd`, web `resunova-web` main `3744e6b`→`e62fe5f`, 2026-06-17)** — Owner-authorized full prod cutover. **API:** `main` is unprotected → direct FF push `git push origin 759befd:main` (44 commits: interview-prep, question bank, jobs system, cover-letter route, tb-generate, gap-fix/jd-match, anon-scan-limit, etc.). Railway prod auto-deploys `main`. **Web:** `main` is a PROTECTED branch (direct push declined) → opened PR [#2](https://github.com/parthbhodia/resunova-web/pull/2) staging→main and merged with `gh pr merge 2 --merge --admin` (105 commits incl. the parallel agent's just-merged "builder AI suite"). GitHub Pages deploy run **succeeded** (verified via `gh run`); `www.resunova.io` → 200. **ROLLBACK POINTS (all prod DB changes were additive, so reverting code is clean): api `git push origin f94ec60:main --force-with-lease`; web `git push origin 3744e6b:main --force-with-lease`** (web needs `--admin`/PR). **VERIFICATION GAP:** could NOT probe the prod API directly — its URL is a Railway/GitHub secret (`RAILWAY_API_URL`) not in any file; the railway-generated `…up.railway.app` guesses 404'd and the prod service name in the `production` env of project `serene-playfulness` isn't `resunova-api-staging`. **Owner must confirm the prod API deploy** (Railway dashboard, project `serene-playfulness` env `production`) and smoke-test `www.resunova.io` → Interview Prep once CDN propagates. Railway/Pages both keep the last-good deploy if a build fails, so a failed deploy won't take prod down. **Prod env vars still needed** (owner; I'm barred from entering API keys): BREVO_*/RESEND_* + any missing LLM keys (Railway Raw Editor, staging→prod). **Jobs feed will be EMPTY on prod** until the corpus lands (see next entry).

- **PROD jobs data copy: H1B done, job_postings BLOCKED by staging exhaustion (2026-06-17)** — Paused the staging launchd cron (`io.resunova.jobs-{scan,extract}` booted out) to relieve staging. Copied **h1b_employer_summary = 30,103 rows staging→prod** via a service-key PostgREST script (`/tmp/copy_jobs_to_prod.py`, keyset paginate + upsert). **job_postings (216K) FAILED:** even with the cron off, staging's PostgREST can't serve the big table (PGRST002 on both the exact-count AND the keyset page reads; h1b worked because it's small). So the corpus can't be copied via PostgREST/MCP. **Options to populate prod job_postings:** (a) `pg_dump --data-only -t job_postings` staging→prod over the DIRECT DB connection (needs the DB password from the Supabase dashboard — owner runs it; pg_dump bypasses the dead PostgREST); (b) re-scan FRESH on prod — `full_scan.py` loads `linkedin_agent/.env` (=prod) and runs `run_scan` in-process against prod, no staging dependency (cleaner, fresher; ~$ extraction). The cron is currently PAUSED — repointing it to prod (direct `full_scan.py`/`scan_worker.py`, since the prod API path needs the deploy) would self-populate prod going forward.

- **PROD DB: interview-prep + question-bank data layer applied to PRODUCTION Supabase `eiumlptnsmowvkxucprl` (2026-06-17)** — Owner explicitly authorized prod ("move everything to prod", premium plan) + connected the prod MCP. **Prod MCP access: server `mcp__e82128aa-923d-4261-ac51-93022f187902__*` → project `eiumlptnsmowvkxucprl` ("Resume Builder"); the original `mcp__supabase__*` → staging `uvnncrtulyezsylcxnhw`. ALWAYS pass `project_id` explicitly; never cross them.** Applied to prod (additive, idempotent — **zero live-site impact, no deployed prod code reads these yet**): (1) created `interview_prep_sessions` / `interview_prep_questions` / `interview_prep_stories` (the per-user tables; prod already had `interview_question_bank` from the owner running my table SQL, schema verified exact match incl. read policy + dedupe index); (2) **seeded `interview_question_bank` with 7,944 real LeetCode questions across 710 companies** (via `import_question_bank.py --apply` with prod creds from `linkedin_agent/.env` + `ALLOW_PROD=1`, plain insert since the table was empty). Verified: bank 7944 rows / 710 companies / google=50; all 3 prep tables present. **Notable prod state discovered:** prod ALREADY has the full **jobs schema** (`job_postings`/`job_scan_runs`/`job_post_events`/`job_applications`/`referral_contacts`/`saved_job_filters` — all **0 rows**), `template_builder_resumes` (0), and `interview_question_bank`. Prod is MISSING: `cover_letters` table, `user_profiles.notify_prefs` column, and all jobs DATA (the 216K postings live on staging). **Staging is resource-exhausted** (PGRST002 schema-cache failures + connection timeouts all session — root-caused to the jobs pipeline hammering it: 216K rows + 5-min full-table cron); prod (premium) is healthy. **PROD DB SCHEMA NOW AT FULL PARITY WITH STAGING (2026-06-17, continued):** found prod's jobs schema was ~17 cols behind staging (`job_postings` 22→39: added extraction_claimed_at, company_domain, work_model, seniority, visa_sponsorship, salary_*_jd, facts_extracted_at, 5×h1b_*, jd_hash, applicant_count) + missing the `claim_extraction_batch` RPC + facet/claim/browse indexes + `h1b_employer_summary` table + pg_trgm — all applied to prod from the idempotent migration files (012–027 `job_*`); also `job_scan_runs.stale_swept`. Plus `cover_letters` table + `user_profiles.notify_prefs` column. Verified prod==staging col counts on all jobs tables. **Why this mattered: deploying the staging code to prod with the OLD jobs schema would have 500'd the live Jobs tab** (queries reference the missing cols) — so schema parity is a hard prereq for the code FF, now satisfied. **STILL PENDING for go-live (3 items):** (1) **216K `job_postings` DATA** → prod — schema's ready so a copy now works, but needs `pg_dump --data-only -t job_postings` staging→prod (DB connection strings/passwords from the dashboard — owner runs it; staging too exhausted to MCP-stream 216K), OR re-extract fresh on prod via the pipeline; pause the staging jobs launchd cron first. (2) **prod Railway env vars** (Brevo/Resend for email triggers; jobs-extraction LLM keys) — owner. (3) **code FF `staging→main`** on both repos (the irreversible live deploy of ~141 commits incl. unverified parallel work) — awaiting explicit go. **DO NOT copy staging's user-tied tables to prod** (prod has real users: 167 résumés/184 analyses/11 profiles; staging rows are test data tied to different auth.users).

- **Jobs: show ALL requirements + warm "tangy" match UI (web `resunova-web` `0cc99cd` on `staging`)** — Two things in `components/JobDetail.tsx` + `BoostPanel.tsx` + `JobsFeed.tsx`. (1) **Completeness fix:** the job-detail `MatchPanel` hard-capped the requirement chips at `.slice(0,14)` with matched-first ordering — but staging postings average **18.3** extracted `requirement_concepts` (1% physical sample: 64% have >14, max 43), so the *actionable missing* reqs were the first truncated. Now chips sort **required-gaps → required-matched → preferred-gaps → preferred-matched**, default-collapsed to 12 with a "Show all N" toggle (nothing permanently hidden). Pre-scan (no résumé → API `_build_match_payload` returns empty matched/missing but `totalRequirements>0`) now shows a teaser line instead of the broken empty "REQUIREMENTS · 0/N MATCHED" header. `BoostPanel` Step 1 had the same caps (matched 8 / gaps 8) → both got show-all toggles. **Invariant:** the backend returns ALL requirement concepts; the *only* display cap is the frontend collapse-toggle — do NOT reintroduce a hard slice. (2) **Warm "tangy/classy" restyle (younger audience):** missing reqs reframed from red ✗ "fail" → amber **"+ add"** pills, matched stay green ✓, plus a terracotta requirements-progress bar + encouraging copy ("You're 45% there…"); Boost "GAPS" → "ADD THESE TO LEVEL UP"; feed cards swap the flat "N/N requirements" badge for a compact terracotta match-meter. **Color gotcha:** the brand warm color is terracotta `#c4793a` (the Optimize CTA), NOT `--accent` — `--accent` is link-blue `#2f81f7` in `resunova-web` (my first pass rendered blue). Warm tints use `color-mix(in srgb, #c4793a …%, transparent)` over adaptive tokens (`--text`, `--green-ink`) so light+dark both hold — verified both. **QA:** `tsc` clean; verified in-browser (light+dark, detail panel + Boost Step 1 + feed cards) by stubbing the jobs API via `window.fetch` override (local backend has a `job_postings.company_domain`-missing schema drift; feed needs auth). **NOT QA'd with a real auth'd résumé** (matched/missing need a scan). **CONCURRENCY (hit again, per the landmine below):** the shared `resunova-web` checkout was on `feature/tb-import-resume` (a parallel agent's WIP incl. uncommitted CoverLetterBuilder) — landed my 3-file patch via a throwaway `git worktree` off `origin/staging` (patch `git apply --check` clean → commit → `push HEAD:staging`, FF), leaving their working tree untouched. My edits still linger as uncommitted mods in that feature checkout (same 3 files) — discard when ready: `git checkout HEAD -- components/JobDetail.tsx components/BoostPanel.tsx components/JobsFeed.tsx`.

- **Jobs ↔ Interview Prep link + "Prep for interview" widget (api `resunova-api` `5ea1b0f`, web `resunova-web` `56c0994`, both on `staging`)** — Connects the Jobs tab to Interview Prep so a user can prep for a specific posting and see which jobs they've already prepped. **DB migration 018** (applied to staging Supabase `uvnncrtulyezsylcxnhw`): nullable `interview_prep_sessions.job_posting_id text` + index `(user_id, job_posting_id)` — **no FK to job_postings on purpose** (postings churn/expire; the kit should outlive them, id kept as a plain label). **Backend:** `save_prep_session`/`find_existing_prep_session` take an optional `job_posting_id` — when set, the session is keyed exactly by `(user_id, job_posting_id)` for reuse (no fuzzy JD-text match); the generate route reads `job_id`/`job_posting_id` from the body and threads it through; new `get_prep_status_for_jobs(user_id, job_ids)` (2 queries, no N+1) + `GET /api/interview-prep/job-statuses?ids=a,b,c` → `{statuses:{job_id:{session_id,question_count}}}` for the "Prep ready" badge. **Frontend:** `lib/interviewPrepLaunch.prefillPrepFromJob(job)` seeds the prep store from a `JobDetail` (company, title→role, jdText→JD, résumé, `jobPostingId`) then the caller navigates to `/interview-prep/dashboard`; `lib/supabase.fetchJobPrepStatuses()`; **JobsFeed** card gets a `🎤 Prep interview` button (→ `🎤 Prep ready` once a kit exists; statuses loaded for the visible feed) next to `✦ Optimize`; **JobDetail** gets a "Prepare for your interview" card in the right column with the CTA + `prep ready · N questions` state; `PrepDashboardView` threads `jobPostingId` into the generate body (`job_id`); `interviewPrepStore` gains `jobPostingId`. **QA:** api 13/13 tests (added `get_prep_status_for_jobs` map+count + job-statuses route tests); 72 routes import; web `tsc` clean; webpack prod build clean; job-statuses route verified live on staging (HTTP 401 unauth). **NOT visually QA'd** — the jobs feed/detail need a signed-in session + real job data that can't be exercised headlessly; verify the two widgets + the job→prep→generate→"prep ready" round-trip on the staging deploy. **Deferred (still, vs career-ops):** per-question→story mapping with fit + gap detection; story editing. **Concurrency:** done entirely in throwaway `git worktree`s off `origin/staging` for BOTH repos (the parallel agent is now running several feature worktrees — `tb-import-resume`/`builder-ai-suite`/`two-column-template`); committed exact files + pushed `HEAD:staging`, clean FF both. (Migration files now: 017 stories-detach, 018 job-link.)

- **Interview Story Bank made a real accumulating master bank (api `resunova-api` `14ae984`, web `resunova-web` `92e7897`, both on `staging`)** — Follow-up that makes the story bank match the [santifer/career-ops](https://github.com/santifer/career-ops) concept ("5-10 master STAR+R stories that accumulate across evaluations"). As first shipped, `interview_prep_stories` was **write-only** (nothing read it back) and **per-session** (`save_prep_stories` deleted by session then re-inserted) — so it never accumulated and was never surfaced. Changes: (1) **`save_prep_stories` now accumulates at the USER level** — no per-session wipe; dedupes candidate stories by normalized title against the user's existing bank; caps the bank at `MASTER_STORY_CAP = 10` (tops up only with genuinely new stories). (2) New persistence `list_user_stories(user_id)` + `delete_user_story(story_id, user_id)`. (3) New routes `GET /api/interview-prep/stories` (returns the bank; 401 unauth) + `DELETE /api/interview-prep/story/{story_id}` (curation; owner-scoped). (4) **Web:** `lib/supabase.ts` `fetchStoryBank()` + `deleteStory()` + `PrepStory` type; a **"Your Story Bank" panel** on `PrepDashboardView` above the question sections — lists master stories (`N/10`) with expandable STAR+R + per-story delete, refreshes after each generation (panel self-hides when empty). (5) **DB migration 017** (`web/db/migrations/017_interview_prep_stories_detach.sql`, applied to staging Supabase `uvnncrtulyezsylcxnhw` via `execute_sql`): `interview_prep_stories.session_id` FK **CASCADE → SET NULL** so deleting a prep session no longer wipes master stories (the `user_id` FK keeps its CASCADE to auth.users). **QA:** api 10/10 tests (added accumulation cap+cross-session-dedupe + list/delete route tests); app imports (71 routes); web `tsc` clean; populated Story Bank panel verified via prod build + static-serve using a temporary `NEXT_PUBLIC_STORYBANK_DEMO` seed (reverted before commit). **Still gap vs career-ops (deferred):** the per-question→story *mapping with fit rating + gap detection* (career-ops Step 5) is NOT built — the dashboard still shows a fresh per-question `best_story` rather than mapping questions against the standing bank. Story editing is delete-only (no edit yet). **CONCURRENCY LANDMINE (this session):** both `resunova-api` and `resunova-web` are SINGLE shared checkouts that a **parallel agent session was actively using** — it ran `git add -A` + switched branches mid-session. Result: (a) my api story-bank edits got swept into the concurrent session's commit `14ae984` ("feat(tb-generate)…") — so that commit's message under-describes its content (it also contains the whole story-bank backend); pushed as-is (forward-only, no rewrite — rewriting a shared checkout's history would break the other session). (b) On web, the checkout had been switched to branch `feature/tb-import-resume`, so my `git commit` landed there (stacked on their unpushed tb-import commits) instead of `staging`; recovered by cherry-picking my commit onto `origin/staging` in a throwaway `git worktree` (→ `92e7897`), leaving their branch untouched. **Lesson: in a shared checkout with a concurrent agent, never assume the current branch; commit your exact files and verify `git log`/branch before push, and use a worktree to land on `staging` without touching their branch.**

- **Interview Prep (STAR framework + story bank) merged to staging — backend + web (api `resunova-api` `02270d4`, web `resunova-web` `79a6250`, both on `staging`)** — Same two-branch pattern as cover-letter: backend `feature/interview-prep-backend` (1 commit, +1467, purely additive — `routes/interview_prep.py` + `services/interview_prep_persistence.py` + tests + import blocks in `routes/__init__.py`/`_shared.py`) and web `feature/interview-prep` (3 commits, +3926 — `app/interview-prep/{,setup,interview-type,dashboard}/page.tsx` dedicated routes, `components/InterviewPrep/*`, `store/interviewPrepStore.ts`, `lib/{keywordExtraction,resumeCategoryClassify}.ts` + `lib/supabase.ts` prep CRUD, AppShell/AppSidebar/nav-icons nav entry, `next.config.ts` turbopack-root tweak, 3 migrations). API was 65 behind staging, web 78. **Merges were clean** (web auto-merged with zero conflicts; api had one import-block conflict in `routes/__init__.py` — kept staging's notify_prefs/cover_letter imports + the new interview_prep imports). **Routes:** `POST /api/generate-interview-questions` (LLM → 4 sections resume/jd/behavioral/company, each Q carries `reason` + `star_framework`{situation,task,action,result,reflection} + `best_story`; honors `sources`/`focus_areas`; anonymous returns questions but does NOT persist), `GET /api/interview-prep/latest` (401 if unauth), `DELETE /api/interview-prep/session/{id}`. Uses `_authenticated_supabase_user` + `_supabase_table` (correct resunova-api conventions). **DB:** 3 migrations applied to **staging** Supabase `uvnncrtulyezsylcxnhw` — `interview_prep_sessions` / `interview_prep_questions` / `interview_prep_stories`, all RLS-on (`auth.uid()=user_id`; questions via session subquery), with 013 addendum (`question_count`, `reason`, indexes) + 016 (stories table + `star_framework`/`best_story` jsonb on questions). Persistence layer writes via service-role (bypasses RLS, like `_persist_analysis`); the web client only calls the latest/delete routes (bearer-authed) — sessions/questions/stories are written server-side. **Two bugs fixed during integration:** (1) **web `79a6250`** — `PrepDashboardView` POSTed to `/api/generate-interview-questions` with NO `Authorization` header, but backend persistence is gated on `user_id`, so nothing was ever saved and "resume your latest prep session" always returned null. Now attaches the Supabase `access_token` (anonymous still generates, just unsaved). (2) **api `02270d4`** — `test_interview_prep.py` patched stale names `upsert_prep_session`/`load_latest_prep_session`; shipped persistence API is `save_prep_session`/`get_latest_prep_session`. **QA:** api 6/6 tests green + app imports (68 routes); `/api/generate-interview-questions` real E2E (HTTP 200 ~14s, returns proper STAR+story JSON, respects sources); full CRUD round-trip on staging DB (jsonb + text[] + cascade-delete, self-cleaned); web `tsc --noEmit` clean; **UI verified via prod build + static serve** (`next build` → 4 interview-prep routes prerendered, 0 errors → `python -m http.server out/`): step-1 (upload/JD/Target-Role chips) + Prep Dashboard (STAR+R disclosure cards, best-story badges, Download/Save/Regenerate bar) render and are interactive; nav entry present in sidebar and **unlocked** (anon-accessible, like Template Builder). **LANDMINES for next session:** (1) **Dedicated `app/*` routes can NOT be QA'd in `next dev`** — both turbopack AND `next dev --webpack` hang forever on the page's `<Suspense fallback>` (the known-good prod route `/template-builder` reproduces it identically) because these routes wrap `<AppShell>` (which calls `useSearchParams`) in Suspense; the rest of the app is a `/?view=` SPA that hydrates fine. To verify a dedicated route, `next build` + serve `out/`. (2) **Local backend points at PROD Supabase** — `linkedin_agent/.env` `SUPABASE_URL=eiumlptnsmowvkxucprl` (prod) while the web `.env.local` points at staging `uvnncrtulyezsylcxnhw`; so local persistence E2E is impossible and any local server-side write goes at prod (no-op here only because prod lacks the tables — do NOT create interview_prep tables on prod without explicit go). (3) `apply_migration` MCP kept timing out at its history-table init — used `execute_sql` (idempotent DDL) instead, then `notify pgrst, 'reload schema'` so PostgREST/supabase-py sees the new tables. Migration file numbers 012/013/016 collide with existing web migrations (job_postings/cover_letters/etc.) — DB doesn't care; renumber if reconciling history. (4) **PROD untouched** — prod Supabase lacks the 3 tables, prod Railway/Pages `main` unchanged; go-live needs the migrations on prod Supabase + a web `main` release.

- **Cover Letter Builder responsive layout fix (web `resunova-web` `3e1fee5` on `staging`)** — The builder shell assumed a wide desktop and broke at narrow widths: a fixed-height (52px) header with no wrap + `overflow:hidden` clipped the "Cover Letter Builder" title and collided it with the Change Template / Prefill / DOCX / PDF buttons; a fixed 340px form panel + an 8.5in (816px) preview overflowed/clipped below ~1100px. Fixes in `components/CoverLetterBuilder/index.tsx`: (1) **header** → `flexWrap` + `minHeight` (title truncates with ellipsis, button group wraps to a 2nd row on mobile); (2) **body** → a `ResizeObserver` on the builder root (robust to sidebar state, unlike the tablet-only `useAppBreakpoints`) drives `narrow = width < 880`; below it the form + preview stack vertically and the pane scrolls as one; (3) **preview** → scale-to-fit (clamped 0.42–0.85) inside a *sizer* wrapper that reserves the scaled footprint (`PAGE_PX*scale × PAGE_H*scale`) with `transform-origin: top left`, so the page stays centered and never overflows at any width. **Gotcha hit & fixed:** the ResizeObserver `useEffect` was first written with `[]` deps, but `if (!loaded) return null` makes the first commit render nothing → the effect ran once while `rootRef.current` was still null and never re-attached → `containerW` stuck at its 1200 default (layout never went narrow). Fix: deps `[loaded, step]` so it re-attaches when the root div actually mounts. (The dev-only "deps array changed size" fast-refresh warning was just HMR from inserting a hook ahead of existing ones — gone on a fresh runtime.) **Invariant preserved:** PDF export still reads the raw 816px page via `previewRef` (sizer + transform are ancestors) — verified `POST /api/export-pdf-html → 200`. Verified across desktop (side-by-side)/820px/375px (stacked, no horizontal overflow), no console errors, `tsc --noEmit` clean.

- **Cover Letter Builder merged to staging — backend + web (api `resunova-api` `dd8c5f8`, web `resunova-web` `990ddf9`, both on `staging`)** — Shipped the dormant cover-letter feature from two feature branches. **DB:** applied `012_cover_letters.sql` (from `resunova-web` branch `feature/cover-letter-builder`, NOT the api branch — the api branch only has the route) to **staging** Supabase `uvnncrtulyezsylcxnhw` via `apply_migration`. Creates `public.cover_letters` (id/user_id→auth.users cascade/label/`data` jsonb/is_default/timestamps), `(user_id, updated_at desc)` index, partial-unique one-default-per-user index, RLS + 4 owner policies — mirrors `011_template_builder_resumes.sql` exactly. Idempotent (`if not exists`/`drop policy if exists`). **Backend** (`resunova-api` branch `feature/cover-letter-builder-backend`, was a clean FF — 1 ahead/0 behind): adds `resume_gui/routes/cover_letter.py` = **stateless** `POST /api/cl-enhance` (AI field-rewrite for opening/whyCompany/whyFit/closing via `_llm_json_call`; auth-gated; **touches no DB table** — the `cover_letters` table is written by the web client via supabase-js, not this route). **Web** (`resunova-web` branch `feature/cover-letter-builder`): the `CoverLetterBuilder/` component tree + `store/coverLetterStore.ts` + `lib/supabase.ts` CRUD + `ResumeLibrary` cover-letter cards + `?view=cover-letter` route. **The web branch was 41 commits behind staging** — rebased onto current staging (only conflict: import block in `HomePageClient.tsx`, kept both Jobs + CoverLetter imports), then **two divergence type-breaks fixed** (commit `990ddf9`): (1) `coverLetterStore.prefillFromProfile` read `firstName`/`lastName`/`linkedinUrl`/`location` off `ProfileFormState` — current shape is `displayName`/`linkedin`/`locations`; remapped. (2) `ResumeLibrary` card read `item.record.pdf_url` in the union branch shared by `tailored`+`cover_letter` — guarded with `item.kind === "tailored"` (runtime-equivalent). `tsc --noEmit` clean before push. **Invariants/notes:** (1) **filename number collides** — web staging already has `012_job_postings.sql`; renumber the migration to `021_cover_letters.sql` before this branch's history is otherwise reconciled (DB doesn't care, applied fine). (2) Rebase/merge was done in a throwaway `git worktree` to avoid disturbing uncommitted docs in the web checkout. (3) **NOT browser-verified** — merged + typechecked only; QA the cover-letter flow on the staging web deploy. (4) Staging-only; prod untouched (prod Supabase lacks the column, prod Railway lacks no new env — route needs only the existing LLM key).

- **Jobs extraction un-stalled: `datetime` shadowing UnboundLocalError in `run_scan` (api `resunova-api` `13970ac` on `staging`)** — The extraction burn silently died (~25 min, 53K recent postings stuck pending despite all having `jd_text`). Every `POST /api/jobs/scan` (extract drain) returned **HTTP 500 in ~0.16s**: `cannot access local variable 'datetime'`. Root cause: the stale-sweep block from `2f3b20e` added `from datetime import datetime, timedelta, timezone` **inside** `run_scan` (`resume_gui/jobs/scan.py`); a function-local import binds those names for the WHOLE function, so the earlier `now_iso = datetime.now(timezone.utc)` at line 202 raised `UnboundLocalError`. The Mac launchd drain (`io.resunova.jobs-extract`, `StartInterval=300` = every 5 min) logged "no bodies, wait 30s" — a shell heuristic meaning *no curl response carried `{"extracted":N}`* (the 500s), NOT an empty backlog. **Fix:** deleted the redundant local import (module-level line 18 already provides all three). Verified post-deploy: `extract_limit=25` → `extracted:24, extraction_failures:0, errors:[]`. **Invariants/notes:** (1) never re-import `datetime` inside `run_scan` — it shadows the module-level binding across the whole function; (2) the **781 stuck-claimed** rows (`extraction_claimed_at` set, concepts null) self-heal — the `claim_extraction_batch` RPC re-claims after its stale window, so no manual reset; (3) **known follow-up:** the global `_stale_sweep` runs an unbounded full-table `UPDATE … WHERE is_active AND last_seen_at < cutoff` on *every* call and intermittently hits the statement timeout (caught, non-fatal, `stale_swept:0`) — should be chunked + indexed on `(is_active, last_seen_at)`, or gated to the daily full scan instead of every 5-min extract drain.

- **Rename "Boost" → "Optimize" on the résumé-tailor CTA (web `resunova-web` `c8b6976` on `staging`)** — User-facing label only: the feed-card button ("✦ Optimize"), the slide-over header ("Optimize my résumé"), and the detail-page CTA. Internal identifiers (`BoostPanel`, `openBoost`, `BoostResult`) unchanged — no user-facing reason to churn them. Verified in preview (both cards + panel header), `tsc` clean.

- **Boost button on every job-feed card via shared `BoostPanel` (web `resunova-web` `4049ef6` on `staging`)** — The 3-step "Boost my résumé" slide-over (Step1 See your difference / Step2 Align / Step3 Review+download) was previously inlined in `JobDetail.tsx` and reachable only from the job detail page's dark match panel. Extracted it verbatim into `components/BoostPanel.tsx` (default export; byte-identical flow incl. `/api/jobs/boost` call + Chromium PDF export) and added an amber **✦ Boost** button (`#c4793a`, matching the match-panel accent) to **every** card in `JobsFeed.tsx`, stacked above the existing "View & apply ↗" link. Because `FeedJob` only carries summary fields, the card's `openBoost(id)` calls `fetchJobDetail(id)` on demand (needs `jdText`/`resumeText`/`structuredResume`/`injectableKeywords`) then mounts the slide-over **in place** — `e.stopPropagation()` keeps the card's detail-page navigation separate, so the detail page stays its own deep-link. Shared `scoreLabel()` moved to `lib/jobsApi.ts` (was duplicated). A dismissible fixed toast surfaces detail-fetch errors. **Invariant:** the Boost slide-over is launched the same way from both the feed card and the detail page — both mount the one `BoostPanel`; do not re-fork the step flow. Verified in preview (stubbed feed/detail fetch under dev-bypass since the staging feed needs a real auth'd résumé): both cards render the amber Boost button, click opens the slide-over over the dimmed feed at Step 1 with the match diff, no navigation; `tsc --noEmit` clean.

- **Resend transactional triggers wired (api `resunova-api` `ebd83d0` on `staging` only)** — The dormant Resend templates now fire on events via `resume_gui/services/email_triggers.py` (fire-and-forget). (1) `maybe_send_scan_limit_warning` — called in **both** 429 daily-limit branches (`/api/analyze-upload` + `/api/analyze`); signed-in + opted-in (`notify_prefs.scanLimit`) only, anon skipped, **deduped once/UTC-day/user** via a process-local in-memory set (resets on redeploy — worst case one dup, like the anon-scan cap). (2) `maybe_send_welcome_on_first_analyze` — called in `/api/analyze-upload` **before** persisting the row so the zero-prior-`resume_analyses` check is accurate; only sends on confirmed-zero priors (lookup failure → skip). Gating + dedup unit-tested with mocks (1 scan + 1 welcome; all negative cases skip). **Invariant: needs `RESEND_API_KEY`/`RESEND_FROM_EMAIL`** (set on staging + prod Railway). **PROD does NOT have this commit** — `origin/main` stays `cf1e3e0`; triggers are staging-only pending QA. Marketing campaigns to Brevo list #3 are still sent manually from the Brevo dashboard (the app only maintains the subscriber list).

- **Tabbed Profile in `resunova-web` (web `6cdbb8e` on staging)** — Gave the live Profile the tabbed look (Dashboard / Career Profile / Settings) that was designed in the dead monorepo's `web/components/profile/*` but never deployed. **Did NOT import those dead-repo components** — they're a mock-data prototype (`CareerProfileTab` hardcodes values, `DashboardTab` needs `/api/profile-dashboard` which isn't in `resunova-api`) and would have regressed the live jobs-onboarding chips + Analyze prefill. Instead **reorganized the existing `resunova-web/components/ProfilePage.tsx` (1913 lines) into 3 tabs**, preserving 100% of its logic (autosave, prefill, onboarding, `ROLE_SUGGESTIONS` chips, EEO, `upsertUserProfile`): an inline-styled tab bar + `profileTab` state gate the form-phase render. **Dashboard** = `<ScanUsageCard/>` + profile-strength meter + Quick actions links (built from existing client data, no new backend); **Career Profile** = the full editable form (identity + Contact/tagline/Job preferences/Education/Tailoring/EEO) + sticky Save/Discard + sparse-hint/import/matchCoach banners; **Settings** = Email preferences (backend-wired) + Visibility. Default tab = Dashboard. **Invariant:** the tabbed Profile is a *render regrouping* of the existing page — no state/effect/helper was rewritten, so autosave/prefill/onboarding behave identically. Verified in preview: tabs swap content correctly, no console errors, email checkboxes inline. Staging-only; prod web is a separate 32-commit release (see prior entry).

- **Email marketing: Brevo + Resend integration — DEPLOYED TO STAGING (api `resunova-api` `cf1e3e0`, web `resunova-web` `155dca9`)** — Two-service email stack. **Brevo** (marketing) — `resume_gui/services/brevo.py` `subscribe()`/`unsubscribe()`/`sync()` against the contacts API (env: `BREVO_API_KEY`, `BREVO_MARKETING_LIST_ID`; live list ID = **3**, "Resunova Users"). **Resend** (transactional) — `resume_gui/services/resend_email.py` `send()` + pre-built `send_scan_limit_warning` / `send_welcome` templates (env: `RESEND_API_KEY`, `RESEND_FROM_EMAIL`; the prod key is send-scoped). These two `send_*`/Brevo helpers have **no triggers wired yet** — Brevo fills list #3 only via the prefs checkbox; Resend templates are dead code until hooked (intended: `send_scan_limit_warning` in the analyze `429` branch gated on `scanLimit` pref; `send_welcome` on first analyze). **Route** — `resume_gui/routes/notify_prefs.py` `GET/POST /api/profile/notify-prefs`: persists `{accountChanges, scanLimit, features}` to `user_profiles.notify_prefs` (JSONB), then syncs the `features` flag to Brevo. **DB** — `notify_prefs` JSONB column applied + verified on the **staging** Supabase project `uvnncrtulyezsylcxnhw` (migration also recorded at `web/db/migrations/012_notify_prefs.sql`). **Invariants:** (1) all Brevo/Resend calls are fire-and-forget — failures logged, never raised, so a down provider never breaks pref-saving; (2) Brevo requires "Authorised IPs" **disabled** in account security (Railway egress IPs are dynamic — verified end-to-end live: subscribe → 201, unsubscribe → 204).
  - **CRITICAL repo-topology note (learned the hard way this session):** `resume-scoring-ai` (this working dir) is the **deprecated dev monorepo** — per `resunova-api/CUTOVER_CHECKLIST.md` it is meant to be archived. **Actual deploys come from two separate repos:** `parthbhodia/resunova-api` (API → Railway project `serene-playfulness`, staging env = `resunova-api-staging`, **branch `staging`**; **`main` = PRODUCTION**) and `parthbhodia/resunova-web` (web; `_webnext` symlink → `/Users/mslcomx/Projects/resunova-web`; **branch `staging`** = web staging, **`main` = prod GitHub Pages**). The local preview server runs `resunova-web`, NOT `web/`. So **code authored in `resume-scoring-ai` must be ported** to those repos to ship. The two backends have **diverged** (resunova-api has the whole `jobs/` system; resume-scoring-ai has `profile_dashboard.py`); they are NOT a clean fast-forward of each other. resunova-api auth API differs too: use `_authenticated_supabase_user(request)` + `_supabase_table(name)` (NOT `get_current_user`/`get_supabase_client`).
  - **What shipped where:** backend ported to `resunova-api` and pushed to **both** `staging` (correct) **and** `main` (mistake — `main` auto-deploys PRODUCTION; the email code is inert in prod since no prod frontend calls it, prod has no Brevo/Resend keys, and prod Supabase `eiumlptnsmowvkxucprl` lacks the column — left as-is per owner's call). Frontend: rather than swap in the new tabbed `ProfilePageClient` (which would have **regressed** the live jobs-onboarding role chips + Analyze prefill that exist only in `resunova-web/components/ProfilePage.tsx`), the existing localStorage-only "Email preferences" Card in that file was **wired** to the backend (load on mount, debounced POST on toggle, Saving/✓ Saved status). That card uses inline `width:14` on its checkboxes so it is immune to the `input{width:100%}` global-CSS bug.
  - **Outstanding for PRODUCTION go-live** (not done — needs explicit owner go): (a) apply the `notify_prefs` column to the **prod** Supabase `eiumlptnsmowvkxucprl`; (b) set the 4 env vars on the prod Railway env; (c) promote `resunova-web` staging→main once QA'd. The separate `web/components/profile/SettingsTab.tsx` (new tabbed Profile) + `c33e480` globals.css checkbox fix live only in the deprecated `resume-scoring-ai/web` and are NOT deployed anywhere.

- **Profile-aware jobs feed (api `369b687` on main, web `ec0616f` on staging)** — `api_jobs_feed` now fetches `user_profiles` (roles + locations) for the authenticated user and passes them into `_build_feed`/`_score_posting`. Each scored job gets `titleMatch` (job title contains a user target role keyword) and `locationMatch` (job location matches a preferred location). Sort order: (role+location match) > (role match) > unmatched, then by `matchScore`. Feed response includes `profileRoles`/`profileLocations` so the frontend knows what was applied. Frontend (`JobsFeed.tsx`): new `rolesOnly` filter state, "🎯 Your roles" filter pill (visible only when profile has saved roles), "🎯 Target role" badge on matching cards. `JobFeedItem`/`JobFeedResponse` exported types in `jobsApi.ts`. **Invariant:** profile lookup is non-fatal — if `user_profiles` is missing or the table is unreachable, feed falls back to pure matchScore sorting. Also ships `JobSearchActivationWidget` (web `91b4451`) shown once post-scan to signed-in users with empty `roles` field — chip-select for target roles + location, saves to profile, fires toast directing user to Jobs tab.

- **Anonymous free scan + locked report funnel (split repos: web `0192d8b` on staging, api `8cfdf3a` on main)** — Signed-out visitors can run one Analyze scan from the landing CTA ("Scan your résumé free — no sign-up" → full navigation to `/?view=analyze`). **AuthGate** lets that URL through via a `window.location` read — NOT `useSearchParams`, which would replace the prerendered landing HTML with a Suspense fallback in the static export (SEO invariant). After the scan, anonymous users see `AnonReportTeaser` (real overall + category scores; flagged-bullet/issue/ATS counts, PDF export, history listed but locked) instead of the results workspace; sidebar/bottom-nav lock Library/Profile/Jobs/Tailor behind sign-in (`anonMode` in AppShell = `authChecked && !user`). The finished result is continuously stashed to localStorage `rn_anon_analysis_v1` (`lib/anonScan.ts`) and restored+persisted on the post-OAuth mount, so sign-in lands on the already-finished report. **Backend backstop** (`resunova-api`): per-IP in-memory daily cap for anonymous requests (`ANON_SCAN_DAILY_LIMIT`, default 2) on `/api/analyze-upload` + `/api/analyze`; same 429 shape with `reason: anonymous_daily_ip_limit`; `/api/scan-limit-status` reports the anon quota. **Invariants:** (1) client IP = LAST X-Forwarded-For entry — earlier entries are client-spoofable; (2) anon scans count only after upload validation so a bad file never burns a slot; (3) the localStorage flag/stash is UX only — enforcement lives server-side; (4) Template Builder stays public. Not live-tested: the real Google OAuth round-trip restore (verified by parts; smoke-test after deploy).

- **Every flagged bullet ships a real rewrite (`510e8f3`)** — The comprehensive analysis was surfacing a usable rewrite on only ~1 of 7 flagged bullets; the rest came back blank and leaned on the in-preview "Generate AI rewrite" button. Two root causes, fixed together. (1) **Prompt** (`_ANALYSIS_PROMPT` in `analysis/comprehensive.py`): replaced "omit if you can't improve substance" with **EVERY FLAGGED BULLET MUST CARRY A REWRITE — else DROP it from `bulletAnalysis`**, plus a per-`primaryCategory` REWRITE RECIPE (quantification → `[X%]`/`[$Y]`/`[~N]`/`[N×]` placeholder; achievementQuality → verb+scope+outcome collapsing a duty list; readability → condense to one line; languageQuality → strip pronouns; sectionStructure → tighten). readability/languageQuality rewrites may now be **shorter** (condensing is the point); achievement/quant stay ≥ length. (2) **Filter** (`analysis/rewrite_validators.py`) was the real bottleneck — it was dropping the LLM's good condensing rewrites: `_substantive_preservation_ok` now gives `quantification`/`quantify_impact` the same `0.40` keep-floor as `achievementQuality` (a quant fix collapses a duty list while adding a placeholder, so it *should* drop enumerated activities), and `_validate_rewrite_against_original`'s proper-noun guard went from reject-on-any-drop to a **keep-ratio** (≤2 names → keep all; else keep ≥60%). **Invariant: numerals stay strictly protected — a condensing rewrite may drop a descriptive proper-noun aside but NEVER a number.** Empirical (judged each rewrite by hand): Nityanshi CV 1/7→6/7 real rewrites, Parth tech CV 3/3 (removed "our"/"I", added `[X%]`), **0 numeral-loss across both**. Remaining blanks still fall through to the Generate button (which bypasses this filter). +3 regression tests (`TestCondensingRewriteRelaxation`); full suite **197 passed**.

- **Hybrid analysis prompt: json_schema mode + ~8,308 token savings (`uncommitted`)** — Stripped the ~8,590-token verbose JSON block from `_ANALYSIS_PROMPT` in `analysis/comprehensive.py`; schema is now enforced at the API level by passing `_ANALYSIS_SCHEMA` to `_llm_json_call` via a new optional `schema` param (switches Grok from `json_object` to `json_schema` response format). Gemini fallback path unchanged. Added explicit bullet-count instruction to the prompt to prevent json_schema array conservatism from under-reporting weak bullets. Added hard scoring anchors for `atsCompatibility` (missing contact info → score ≤ 40-55) and `languageQuality` (first-person pronouns / duty phrases → score ≤ 50). Net: 33,234 chars / ~8,308 tokens saved per call. All 79 dimension tests pass. **Invariant:** schema dict `_ANALYSIS_SCHEMA` lives in `comprehensive.py` next to `_ANALYSIS_PROMPT` — keep them in sync if you add/rename output fields.

- **Analyze + Tailor sidebar/nav UX (`632e0fc`)** — Recent Analyses / Recent jobs columns stay open by default (including during scans); app shell nav collapses on scan/analyze start via `useAppShellSidebar` so the workspace gets width without hiding history. Tailor pre-result flow mirrors Analyze’s left column (`rb-tailor-shell`); `TailorRecentJobs` gains empty/loading sidebar modes. Removed tablet-only forced app-nav collapse on first visit. **Invariant:** contextual history sidebar vs app nav are independent — collapse outer nav on scan, never auto-hide the analysis/match column mid-run.

- **Shared font-size presets + project bullet editor (`f7a7dc9`)** — `RESUME_FONT_SIZE_OPTIONS` + `fontSize` on `resumeLayoutCssVarsForPreviewStyle` / `resolveResumeLayout` (0.92× / 1.0× / 1.10×). Template Builder Style tab and Analyze/Tailor `AnnotatedResumePanel` toolbar both use it; Analyze choice persists in `localStorage` (`rn_preview_font_size`). Projects section uses `BulletListEditor` with `minRows={2}` (experience stays 1). **Invariant:** preview font scale is owned by `resumeLayout.ts` so HTML→Chromium export matches the on-screen paper.

- **Analyze flagged-bullet compact UX (`bd4fda6`)** — Category sidebar “Flagged Bullets” cards use `BulletImprovedEditor` `variant="compact"`: collapsed header adds a fix-type chip (e.g. Proofreading / Achievement); expanded body drops per-bullet issue tags + `CATEGORY_REWRITE_HINTS` blockquote, shows readable “Suggested” text with **Apply to preview** / **Copy** / **Edit** (textarea only when Edit). Preview-panel / Tailor paths keep default editor layout. **Invariant:** bullet card teaches fix via suggestion + apply, not triple category copy.

- **Quant 75% target + required placeholder rewrites (`668de41`)** — Analysis prompt requires `categoryRewrites.quantification` / `improvedBullet` with `[X%]`/`[$Y]` when flagging quant bullets; recruiter bar ~75% metric coverage. `normalize` treats bracket placeholders as quant support and backfills `improvedBullet` from `categoryRewrites.quantification`. UI copy + `TARGET_QUANTIFIED_BULLET_SHARE=0.75`.

- **Advisor bug reports (admin-only, `uncommitted`)** — `GET /api/admin/bug-reports` lists `bug_reports` only when `institution_advisors.role = 'admin'` (`global_admin`). `cohort-stats` + `advisor-access` expose `global_admin`; `AdvisorDashboard` shows a Bug reports panel for admins only — UMBC advisors never see it.

- **Deterministic topIssues gated to LLM-fallback only (`668de41`)** — Normal analyze skips `inject_deterministic_insights`; frontend hides `source: "deterministic"` topIssues. Quantification UX = LLM rationales + flagged bullet rewrites only.

- **Deterministic recruiter insights + universal-filler buzzwords (`0f1e0df`)** — `deterministic_insights.py` + narrowed `_BUZZWORDS`; inject originally ran post-LLM (reverted for UX). `test_deterministic_insights.py`. Optional `pyspellchecker` dep.

- **Template Builder cloud save to Resume Hub (`uncommitted`)** — `template_builder_resumes` table (migration `011_template_builder_resumes.sql`) stores named `TBResumeData` drafts per user (RLS). Resume Hub lists them as **Builder drafts**; **Save to Hub** / **Update in Hub** in Template Builder; reload via `/template-builder/?builder=<id>`. Separate from `resume_analyses` and tailored `resumes` rows; localStorage remains offline fallback.

- **Template Builder custom sections (`uncommitted`)** — `TBResumeData.customSections` + `sectionOrder` slots (`custom:<uuid>`) support certifications, awards, volunteering, and arbitrary extra sections. Sections tab: preset chips, drag reorder, hide, inline editor, remove. Prefill maps `structuredResume.extra_sections` (skips per-company Technologies extras). Analyze preview toolbar already has **Edit in Builder** (`501b50e`). **Invariant:** WYSIWYG export still uses `ResumePreview` + `renderSectionSlot`; header/contact fixed at top.

- **Section-feedback pronoun claim sanitizer (`uncommitted`)** — `evidence_validator._sanitize_pronoun_claims_in_text_fields` always runs after ATS non-issue strip: strips "remove personal pronoun" clauses from `sectionFeedback` when that section has no pronouns (Nutri AI / PROJECTS case), and from `topIssues` / `categoryRationales` / `finalRecommendations` when the whole résumé has none. +2 tests (78 in `test_analyze_dimensions.py`). Invariant #13.

- **Participial duty-lead detection + achievement rewrite validator (`uncommitted`)** — `analysis/constants.py` adds `_bullet_leads_with_strong_ownership_verb()` so `Automated testing…` no longer counts as a strong lead while `Automated CI/CD pipelines…` still does; résumé share + evidence validator per-bullet weak-verb drops use it. `rewrite_validators._validate_achievement_rewrite` rejects `achievementQuality` `improvedBullet` / `categoryRewrites.achievementQuality` when the opening word is unchanged or the rewrite still opens with a participial duty phrase (fixes the "same Automated…" false improvement). `normalize._filter_bullet_rewrites` passes `primaryCategory`. +5 tests (76 in `test_analyze_dimensions.py`). Invariants #11–12.

- **App sidebar Contact nav (`71467c4`)** — `/contact` (mailto + Gmail compose to `contact@resunova.io`) was public and in the landing footer but missing from `AppShell`. Added footer menu item + footer text link in `AppSidebar`. **Invariant:** support/legal routes discoverable from signed-in shell, not only marketing footer.

- **Analyze source PDF storage (`a6c641b`)** — Analyze-only students had no PDF in advisor/library because `resume_analyses` stored only the `result` JSON snapshot. `/api/analyze-upload` now uploads the original PDF to the same `resume-pdfs` bucket as Tailor (`upload_analyze_source_pdf` → `{user_id}/analyze-{analysis_id}.pdf`), persists `source_pdf_url` + `source_filename` on `resume_analyses` (migration `008_resume_analyses_source_pdf.sql`), and returns `analysisId` / `sourcePdfUrl` so the frontend skips duplicate `insertAnalysis`. Advisor `student-detail` exposes `latest_source_pdf_url`; Resume Hub analyzed detail embeds the PDF when present. **Invariant:** analyzed library items remain JSON snapshots; the source file is an optional column on the analysis row, not a second `resumes` row. Historical runs lack URLs until re-upload.

- **Advisor roster email resolution (`67f47a6`)** — UMBC scope correctly keyed off `user_id`, but roster labels came from `resume_analyses.user_email`, which was often NULL (frontend `insertAnalysis` omitted it). Added `_email_by_user_id()` in `resume_gui/auth/supabase.py` (institution_students + auth.admin list_users fallback); `/api/cohort-stats` and `/api/student-detail` now hydrate `user_email` before returning roster rows. Frontend `insertAnalysis` persists `session.user.email`. Prod backfill: `UPDATE resume_analyses … FROM auth.users` cleared all blank emails. **Invariant:** advisor display name should never depend solely on analysis-row email when auth email exists.

- **Advisor auth resiliency guardrail (`uncommitted`)** — `web/components/AdvisorDashboard.tsx` now treats `supabase.auth.getUser()` as fallible and still marks auth as checked on failure, preventing the view from stalling in an indeterminate state. Also replaced the silent `if (!data) return null` branch with a user-visible fallback card + retry action so transient cohort-fetch/auth timing issues surface as recoverable UI instead of a blank pane.

- **Free-scan onboarding banner + scan-limit import hardening (`1f48b7b`)** — Added a top-of-shell `FreeScanWelcomeBanner` for signed-in non-UMBC users to clearly communicate the 5 free resume scans/day policy and persist dismissal per user in localStorage. Also made `resume_gui/routes/analyze.py` import `_scan_limit_status_for_user` directly from `services.scan_limits` instead of relying on `_shared` wildcard exports, preventing runtime `NameError` drift in scan-limit enforcement paths.

- **Analyze-upload scanned PDF recovery + scan limits (`b7fcd78`)** — `resume_gui/routes/analyze.py` now avoids early 422 for PDFs with empty markdown extraction: textless PDFs continue into vision extraction, and only return the scanned-PDF error when both text extraction and vision-synthesized text are unavailable. This fixes `/api/analyze-upload` false negatives where scanned/image-only resumes previously failed before vision could recover them. The same commit also enforces daily scan limits in `/api/analyze-upload` and `/api/analyze` with `429` + `{code:"daily_scan_limit_reached", limit, used, resetAt}` metadata.

- **Scanned PDF upload vision fallback (`uncommitted`)** — `resume_gui/routes/library.py` no longer returns `no_extractable_text` (422) immediately when MarkItDown markdown is empty. `/api/upload-resume` now attempts PDF vision extraction first; only hard-fails when both markdown text and vision extraction are unavailable. For image-only PDFs with successful vision extraction, the route skips text parsing, synthesizes preview text from the vision doc, and returns `structuredResume` normally. This prevents false negatives on scanned résumés that previously failed before vision fallback.

- **Analyze → Template Builder structured prefill + entry reordering (`uncommitted`)** — Added a bridge from analyzed library items into `/template-builder/`: `stashTemplateBuilderStructuredPrefillFromAnalysisResult()` maps saved `structuredResume` into `TBResumeData`, stores a one-shot prefill payload, and `TemplateBuilderClient` consumes it on load to hydrate the builder form. This turns Analyze snapshots into editable Template Builder sessions without re-upload/parsing. Builder state now includes `replaceData` plus `moveWork` / `moveEducation` / `moveProject`; the UI exposes up/down ordering controls per entry so users can quickly reorder sections before export. **Invariant:** Template Builder remains the same WYSIWYG HTML→Chromium export path; this change only affects prefill/edit workflow and entry ordering, not PDF rendering semantics.

- **Structured source-of-truth in `/api/analyze` deterministic layers (`0649557`)** — `resume_gui/routes/analyze.py` now synthesizes `resume_for_match` from `structured_resume` when available (`_resume_doc_from_parsed` → `_synthesize_text_from_resume_doc`) and uses that text for both `apply_gap_workflow(...)` and `_run_jd_match_pipeline(...)`. Fallback remains `candidate_profile` when structured data is absent. **Invariant:** scoring + deterministic gap verification/JD matching should read from the same structured-backed résumé view whenever possible, so Tailor re-checks do not drift from the stored structured document.

- **Advisor action-first dashboard + shadcn table/input (`uncommitted`)** — Reframed `AdvisorDashboard` from passive analytics to advisor workflow: KPI strip (students, analyses, average score, needs-attention, tailored resumes), readiness distribution, ranked improvement areas, prioritized "Students needing review" queue, and richer roster statuses/actions (`Needs Work` / `Improving` / `Ready` / `No Analysis Yet`). Added reusable `web/components/ui/table.tsx` and migrated roster/detail tables + search input to shadcn primitives (`Table*`, `Input`) instead of ad-hoc styling. Backend `/api/cohort-stats` now returns `tailored_resume_count` and computes `top_issues` by unique students affected (not raw repeated mentions), making advisor trends action-oriented.

- **Tailor structured preview + gap-fix UX (`9acd185`)** — `/api/analyze` accepts `include_structured_resume` and returns `structuredResume` for text-path extract when Tailor has no PDF vision doc; `saveTailorMatchToLibrary` persists `resume_doc.structured`; Recent Jobs + `intent=job` prefill restore profile + structured before redirect. **`/api/suggest-gap-fix`:** if `structured_resume` is empty but legacy clients send `candidate_profile`, server runs `_llm_extract` once then builds eligible bullets (prod-safe until frontend deploy). **Frontend:** gap-fix cards only in sidebar tab `gapfix` (`GapFixTabPanel`); inline panels removed from qual/resp/keywords/`MatchBreakdownCards`. Preview purple-highlights **original** lines via `tailorGapFixHighlights` — no synthetic `improvedBullet` on panel open. Request sends both `structured_resume` and `candidate_profile` when available. Relaxed `isStructuredUsable` (header OR first employer + body).

- **Structured schema gap-fix (`c2800bd`)** — `/api/suggest-gap-fix` no longer sends plain `candidate_profile`; it requires `structured_resume` and builds an **ELIGIBLE BULLETS** JSON list (`resume_gui/tailor/structured_gap_fix.py`) so project headers (`Name | tech`) are never rewrite targets. LLM output is schema-enforced via `SUGGEST_GAP_FIX_SCHEMA` + `llm/gap_fix_call.py` (Grok `json_schema` strict → Gemini `response_schema` → `json_object` fallback). Prompt is role-neutral (`gap_fix_prompt.py` + `classify_role_family` examples); server truth layer in `gap_fix_validate.py` + `gap_fix_terms.py` (≥1 JD product term per rewrite when honest). Frontend: Fixes tab suggestions always editable (`CategoryFixPanel` + `updateSuggested` in store), responsibility gaps pass `type: "responsibility"`, Recent Jobs refresh via `tailorAnalyzeLibrary.ts` + `RESUME_LIBRARY_CHANGED_EVENT`. **Invariant:** gap-fix LLM proposes patches; validators + structured `original` whitelist decide what ships; user edit/apply is final intent.

- **JD-driven gap verification (`requirement_match`)** — Replaced global `_SKILL_ALIASES` with `resume_gui/tailor/requirement_match/`: Layer 1 JD phrase extraction, Layer 2 dynamic abbreviations + core-phrase aliases, Layer 3 optional `domain_dictionaries/{role_family}.json`, disambiguation safety (`java`≠`javascript`). `POST /api/analyze` passes `job_description` into `apply_gap_workflow` → `verify_gap_in_resume` returns `RequirementMatch` (method: exact/alias/abbreviation/not_found). Frontend types in `web/lib/requirementMatch.ts`. Tests: `test_requirement_match.py` + `test_gap_workflow.py`. **Next:** LLM `extract_requirements_from_jd()` to populate `RequirementConcept[]` at analyze time (not only on gap apply).

- **Shared résumé layout module (`5edc891`)** — `web/lib/resumeLayout.ts` is the single source for style presets, page padding, CSS vars (`resumeLayoutCssVars` / `resumeLayoutCssVarsForPreviewStyle`), TB inline styles (`resumePageRootStyle`, `resumeSectionTitleStyle`, …), and Analyze block spacing (`paragraphBlockStyle`, `bulletsBlockStyle`, `RESUME_BULLET_STYLESHEET`). `ResumePreview`, `AnalyzeLiveResumeBody`, `AnnotatedResumePanel`, and `ResumePDFTemplate` import from here; `templateStyles.ts` is a thin deprecated re-export. **Invariant:** changing spacing/fonts for preview or Chromium export should touch only `resumeLayout.ts` (plus accent chips in `AnnotatedResumePanel`).

- **Per-company Technologies under jobs (`87be5a4`)** — Follow-up to structured section-order fix (`0c9aa38`): résumés that put each employer's stack in `extra_sections` as `Technologies (Company)` / `TECHNOLOGIES - Company` (also `:` separator) no longer render as a trailing orphan section. `buildBlocksFromStructured` and `_synthesize_text_from_resume_doc` partition those extras, fuzzy-match company keys, emit `Technologies: …` under the matching experience entry (after bullets), and only append unmatched extras last. Bare `Technologies` (no company) stays global. Keeps structured preview and `extractedText` in sync. +2 tests in `test_resume_extraction.py`.

- **Structured preview render + Template Builder look (`7eca9ee`, `70e9cec`)** — The Analyze/Tailor preview (`AnalyzeLiveResumeBody`) now renders from the typed `structuredResume` object instead of re-parsing synthesized flat text. New `buildBlocksFromStructured()` emits the same `Block[]` shape from typed fields (header/section/paragraph/bullets), killing the whole class of text-parse bugs (double bullets, project tech-stack-as-bullet, stray markers) at the source. **Critical invariant:** `bulletAnalysis` is a SPARSE weakest-only subset (~15) and `bulletMap` is NOT index-aligned with it — so bullet→analysis identity still uses the fuzzy `findBulletIndexForLine`; most bullets resolve to `-1` and render via a NEW neutral branch (no badge/popup/highlight) instead of being dropped by the old `if (!bullet) return null` guard. `collapseAdjacentSameBulletRows` guarded so `-1` rows never merge. `isStructuredUsable` gate selects the structured builder, falling back to text `buildBlocks` for legacy/restored-history/Word-doc payloads. Analyze reads `structuredResume` from the Zustand store (now typed with `education`/`projects`/`section_order` + defensive `normalizeStructuredResume`); Tailor threads it as an **authoritative** prop through `TailorPreviewPane` → `AnnotatedResumePanel` (`structuredResumeAuthoritative`) so a stale Analyze-store value can't leak in. Phase B restyled the render branches + `PREVIEW_STYLE_OPTIONS` to the Template Builder look: left-aligned name header with ` | `-separated contact, accent-underlined section titles, three TB presets (Classic Times / Modern + Executive Helvetica). All interactivity + WYSIWYG Chromium PDF export preserved (export verified 200 OK). Project tech-stack promotion (`_looksLikeTechStackLine`) ported from `synthesize.py`. **The structured render and Template Builder's `ResumePreview` now produce the same clean layout from the same typed shape — but `AnalyzeLiveResumeBody` keeps its interactivity; do NOT swap in `ResumePreview` (it's static).**

- **Project tech-stack + bullet-marker synthesizer fixes (`fcece15`)** — `resume_gui/extract/synthesize.py`: `_looks_like_tech_stack_line` now detects comma-separated stacks (was `·`-only) and promotes a project's tech-stack first bullet onto the `name | tech` header; `_strip_leading_markers` runs on every experience/project/education bullet so a vision-kept `*`/`•` never doubles with the frontend CSS marker. +2 tests in `test_resume_extraction.py`.

- **`724d8f7`** — Fixed `/api/upload-resume` 500 ("Something went wrong while processing your résumé"): `library.py` used `log_extraction_debug` via the `_shared` wildcard import but it was missing from `_shared.py`'s `__all__`. Added the import + `__all__` entry.

- **Tailor gap-fix apply/rescore (this session)** — Fixed the class of bugs where applied gap fixes didn't update preview/score, only one bullet got a green highlight, and gaps reappeared after rescore. `synthesizeProfileWithBulletOverrides` now fuzzy-matches originals + appends unmatched bullets; `applyGapFixes()` batches Apply All in one rescore; rescore sends `include_bullet_analysis: true` and remaps `tailorLineOverrides`; `mergeRescorePreservingAddressedGaps` keeps addressed gaps in covered after LLM rescore. Green flash uses `tailorAppliedBulletIndices` (Set) not a single index. Helpers in `web/lib/tailorGapFix.ts`.

- **`b034ae8`** — App shell nav polish: custom SVG icons in `nav-icons.tsx` replace Lucide for primary nav items; collapsed sidebar shows icons-only with tooltips while expanded state shows labels/badges. Shared `NAV_MENU_BTN_CLASS` + `globals.css` nav icon sizing. `AppSidebarUser` and bottom nav aligned to the same pattern.

- **`e1c8772`** — Onboarding docs: root `README.md` is now Resunova-focused (local dev, repo layout, link to backend README). `resume_gui/README.md` expanded with directory map, env var table, pipeline diagrams, common tasks, and deploy notes. `web/AGENTS.md` links to backend README for API work.

- **`c91c325`** — Backend refactor phase 2: extracted structured-doc parsing (`doc_normalize.py`, `education_parse.py`, `structured_doc.py`) from `app.py`. Builders (`_resume_doc_from_parsed`, `_build_resume_doc_from_llm_raw`), normalization pass, and JSON serialization now live under `extract/`. `app.py` down to ~6,100 lines; still owns routes, LLM orchestration, and suggestion application.

- **`0d90f5b`** — Backend refactor phase 3: completed monolith split. `app.py` is ~80 lines (Starlette factory + test re-exports). Route handlers in `resume_gui/routes/` by domain; LLM in `llm/`, comprehensive analysis in `analysis/comprehensive.py`, extract orchestration in `extract/pipeline.py`, suggestions in `suggestions.py`, auth in `auth/supabase.py`. Onboarding map in `resume_gui/README.md`.

- **`9e719f9`** — Backend refactor phase 1: extracted analyze honesty pipeline (`resume_gui/analysis/`) and PDF extract helpers (`resume_gui/extract/` vision, synthesize, text_utils, education) from the `app.py` monolith. `app.py` re-exports the same `_`-prefixed names for tests and scripts. Added `resume_gui/README.md` as the onboarding map. Invariant: no behavior change — 87 pytest cases stay green.

- **Chromium-only PDF + Template Builder replaces studio (this session)** — JD Tailor no longer calls LaTeX (`apply-suggestions` / `generate-stream`) for gap fixes or download; preview + export use `useHtmlPdfExport` like Analyze. Legacy `?flow=template` / `ResumeTemplateStudio` redirect to `/template-builder/`. Removed LaTeX template picker from tailor upload form and "LaTeX layout" chips from `AnnotatedResumePanel`; Style + accent swatches on the preview panel are the single styling surface.

- **Landing previews + shadcn padding + auth sign-out (`78a0bdd`)** — Homepage embeds `LandingFeatureShowcase` variants (hero A, animated B/E/D sections with compact headings); card grids + how-it-works hidden via flags; footer simplified; India stat removed from ticker. `signOutAndReturnHome()` + `AuthGate` force-landing fixes sign-out under `NEXT_PUBLIC_DEV_BYPASS_AUTH`. Sidebar footer overflow visible so Account menu isn't clipped. Tailwind `@source` scan paths + removed universal `padding:0` reset restore shadcn Button/Badge padding; component slot fallbacks in `globals.css`. `/landing-preview` shows all variants; Tailor builder embeds variant C.

- **Category score explanations below 95 (this session)** — Any category scored under 95 must show *why*: analysis prompt emits `categoryRationales`; `/api/explain-category-score` backfills saved runs; `AnalyzeResume` auto-fetches on category open when missing. Removed misleading "looks strong" empty state for 70–94 scores and dropped the "no trusted AI rewrite / quality filter" copy on bullet cards. `/api/upload-resume` now vision-synthesizes `extractedText` for PDFs (analyze parity). Tailor results header drops LaTeX "Generate tailored PDF" — download is HTML/Chromium only.

- **Tailor WYSIWYG preview + match sidebar (this session)** — Tailor results use `TailorPreviewPane` → shared `AnnotatedResumePanel` + `useHtmlPdfExport` (same WYSIWYG stack as Analyze). `DetailedRatingsView` split into `TailorMatchSidebar` + `TailorMatchDetail`. `/api/upload-resume` returns vision-synthesized `extractedText` + `resumeHeader` + `structuredResume` for PDF uploads (same text as analyze-upload). LaTeX "Generate tailored PDF" CTA removed from results header — download is HTML/Chromium only.

- **Language micro-edits under Language Quality (this session)** — Trivial rewrites (tense, `;`→`and`, spelling) no longer vanish: backend salvages them into `categoryRewrites.languageQuality`; frontend shows them only under the Language category with a "Proofreading" label. Primary achievement/quant rewrites still require substantive changes. `_BULLET_ANALYSIS_MAX` raised from 8 → 15 weakest bullets in the analysis prompt.

- **Analyze score/UX alignment (this session)** — Fixed the category-score vs bullet-layer mismatch that made Readability look empty while the preview still warned about highlights. Frontend now uses `bulletBelongsToCategory()` (checks `issueCategories` for display, `primaryCategory` for rewrites only). Preview banner/badge gated on actual matches; low-score categories without bullets get explanatory empty state; stripped-rewrite bullets explain the quality filter. Added `resume_gui/experience_tenure.py` + `experienceSummary` on analyze-upload (merged tenure from structured experience dates; chip in sidebar + overview). Docs: [`docs/ANALYSIS_ALGORITHM.md`](docs/ANALYSIS_ALGORITHM.md). Invariant #10. Tests: +6 tenure cases (74 total with dimensions).

- **Template Builder in app nav (this session)** — `/template-builder/` is now wrapped in the shared `AppShell`, so the standalone builder gets the same left navigation shell as the rest of the app. `AppShell` detects the pathname and treats the Resume Builder drawer as active on that route, with a new `Template Builder` sub-item linking directly to `/template-builder/`; `Tailor to a job` remains the normal `/?view=builder&flow=tailor` path. This keeps the public no-signup tool accessible while making it discoverable from the in-app Resume Builder nav.

- **Template Builder WYSIWYG PDF export (this session)** — The Template Builder download now uses the shared HTML → Chromium export path (`useHtmlPdfExport`) against the actual `ResumePreview` DOM node instead of maintaining a separate `@react-pdf/renderer` layout. This keeps preview spacing, page margins, font sizing, and header/contact spacing aligned with the downloaded PDF, matching the Analyze flow's WYSIWYG invariant. `ResumePreview` now forwards a ref to its page root so the export captures the unscaled 8.5in paper element rather than the UI preview wrapper. Verification: `npx tsc --noEmit` passed.

- **Advisor access UX cleanup (this session)** — Added a lightweight `/api/advisor-access` endpoint that reuses `_advisor_scope_for_request` and returns `{ allowed: boolean }` without loading cohort data. `AppShell` now checks this endpoint after Supabase session load/auth changes and only renders the Advisor nav item when the current user is actually an advisor; direct `?view=advisor` visits still render the Advisor page but get a clearer roster-based restricted message with sign-out/check-again actions. Operational note: adding an advisor to the SQL migration does not update live Supabase until the migration is applied; `pbhodia1@umbc.edu` was inserted into `institution_advisors` directly via MCP as an active UMBC test advisor.

- **Advisor dashboard shadcn + UMBC test advisor (this session)** — Refactored `AdvisorDashboard.tsx` toward the repo's shadcn/base-nova primitives (`Button`, `Badge`, `Card`, `Progress`, `Separator`, `Skeleton`) while preserving the existing cohort/student data flow. KPI cards, score badges, loading/auth/error states, cohort overview sections, and student detail panels now use the shared UI layer instead of mostly bespoke `div`/`button` markup. The institution advisor migration now seeds `pbhodia1@umbc.edu` as an active UMBC test advisor and keeps the row idempotent with `on conflict`. Verification: `npx tsc --noEmit` passed; local `npm run lint` still requires Node >=20.9.0 because the current shell is Node 16.

- **Resume Hub + analyzed resumes (this session)** — Library is now a unified **Resume Hub** instead of a generated-resume-only list. The frontend keeps `resumes` and `resume_analyses` as separate persistence tables for v1, then normalizes them through `fetchLibraryItems()` into a `LibraryItem` union (`tailored` vs `analyzed`). `ResumeLibrary` renders both card types with filters (`All`, `Analyzed`, `Tailored`, `Default`); analyzed cards surface score, date, top issues, and actions to open/continue the saved analysis, tailor from extracted text, or export via the Analyze preview. `LibraryResumeDetailPanel` now branches by item kind: tailored keeps the PDF/share/template panel, analyzed shows category scores, issues, strengths, and a live extracted-text preview. `AnalyzeResume` accepts `?analysis=<id>` and restores that saved history row after Supabase history loads. UX invariant: analyzed library entries are saved analysis snapshots, not original uploaded file storage; PDF export still goes through the Analyze preview after opening the saved run.

- **structured bullet categories (this session)** — Root-cause fix for the recurring "misleading hint / wrong rewrite" class of bug. The frontend used to *re-derive* each bullet's category with a ~150-line regex pile (`guessIssueCategory`, `buildBulletPrimaryCategories`, `inferBaseCategory`) and could disagree with what the backend intended — that disagreement is what surfaced quantification hints on `sectionStructure` bullets, dead-end TOP FIXES, etc. **Now the backend is authoritative.** The analysis prompt emits `primaryCategory` (the single categoryScores key `improvedBullet` fixes) and `issueCategories` (every key the bullet is weak in) per bullet. `_normalize_bullet_categories` in `app.py` validates/backfills them and guarantees three invariants: (1) both fields ⊆ `_CATEGORY_SCORE_KEYS`; (2) `issueCategories` always contains `primaryCategory`; (3) **"quantification" appears in neither field unless a surviving rewrite actually adds a numeral** — same evidence test as the issues[] strip, so the category bucket and the tag can never disagree. Frontend `analysisCategoryMatch.ts`: `buildBulletPrimaryCategories` now trusts `primaryCategory` verbatim when every bullet has a valid one (fast path), falling back to the regex heuristics only for legacy restored-history payloads that predate the fields. `getRewriteForCategory` / `bulletMatchesAnalysisCategory` / `inferPrimaryCategoryFromBullet` prefer the explicit field. New invariant #9 (see Validators). 8 new tests in `test_analyze_dimensions.py` (`TestBulletCategoryNormalization`) — suite now 66, all green. **NOTE:** the container's `cryptography` rust binding panics on import of `resume_gui.app`; fixed this session with `pip install --upgrade cryptography` + installing `sse_starlette`, `beautifulsoup4`, `google-genai`. Future sessions running pytest may need the same.

- **`2912c26`** — Fixed misleading quantification hint showing on all flagged bullets. `getRewriteForCategory` in `web/lib/analysisCategoryMatch.ts` had a fallback that returned `bullet.improvedBullet` as the "quantification rewrite" whenever `bulletSignalsQuantificationWeakness` was true — even for bullets whose primary category was `sectionStructure` or `readability`. This made `categoryRewriteBase` non-empty for those bullets, triggering the "Add a number where it helps…" hint alongside an irrelevant rewrite. Fix: removed both `quantification` and `achievementQuality` category-specific fallbacks. A rewrite is now returned only if (a) user has a draft, (b) `categoryRewrites[category]` exists, or (c) primary category matches. All other bullets fall through to the "No auto-rewrite for this one" message. Also removed the now-dead `bulletSignalsQuantificationWeakness` helper.

- **`50de1d9`** — Gated the `CATEGORY_REWRITE_HINTS` render on an actual rewrite being present (`draft.trim() || categoryRewriteBase.trim()`). Added a "No auto-rewrite for this one" fallback message when neither draft nor base rewrite exists. These two commits together (50de1d9 + 2912c26) close the "hint on every bullet" bug.

- **`5284b9e`** + **`9075a03`** + **`f714cf4`** + **`967dfc9`** — Preview→card linking + auto-open first bullet. Clicking a bullet in the résumé preview now opens its suggestion card on the left. Uses `handleBulletSelectFromPreview` which checks `bulletPrimaryCategories[index]` to switch category if needed. `pendingExpandIdxRef` pattern bridges the async React state-change cycle: when a category switch is triggered by a bullet click, the ref stores the target bullet index and the `[activeCategory]` effect reads + clears it. Also: first flagged bullet in a category auto-expands when category is selected. Near-no-op filter in backend: `_word_jaccard(original, rewrite) >= 0.88` → rewrite dropped (catches ";" → "and" style non-changes). PDF clean export: `cleanForExport()` in `useHtmlPdfExport.ts` strips score badges, chevron SVGs, colored `[data-bullet-idx]` backgrounds before sending HTML to Chromium.

- **`5f9678d`** — Template Builder: full UI redesign + mobile Analyze fix. (1) `/template-builder` added to `AuthGate PUBLIC_ROUTES` — it was showing the landing page for signed-out users even though the builder is intentionally sign-up-free. (2) Accordion-based left panel replaced with a 6-tab icon grid (Profile/Experience/Education/Projects/Skills/Style) matching open-resume's two-column aesthetic. Top header bar with Reset + Download PDF. Style tab has font-picker cards and 8 color swatches with live preview. New fields: Website in Profile, Coursework in Education, Tech Stack + Link in Projects — in both HTML preview and PDF template. (3) Mobile Analyze fix: `height: 100%` on both workspace-split flex children was compressing the work slot to ~230px, making analysis unreachable; fixed with `height: auto !important` + `-webkit-overflow-scrolling: touch`. Key invariant: `TemplateBuilderStore` interface is now exported so section sub-components can type-check props without relying on `ReturnType<typeof hook>`.

- **`7d79ddd`** — **Restored analyze-first flow in the tailor builder; removed Accept/Skip + live streaming.** `handleAnalyze` POSTs to `/api/analyze` (fast, ~20s, no PDF compile), gets `RatingsData` back, and sets `result` with `ratings` but `folder/pdfUrl = null`. The `DetailedRatingsView` then renders immediately — user sees match score, gaps, keywords. "Generate tailored PDF →" button in the result header commits to a LaTeX compile. `suggestionsReviewMode` is now hardcoded `false` (panel removed). `showSuggestResearchPanel` hardcoded `false` (live streaming removed). `showBuilderInputs` now gates on `analyzing` too so the form hides while analysis runs. The tailor PDF path is still LaTeX (via `/api/generate-stream`) — HTML→Chromium is Analyze-flow only. **shadcn/ui is installed** (button, card, badge, dialog, progress, separator, skeleton, tooltip in `web/components/ui/`) but ResumeBuilder and the tailor flow still use inline styles — migration is pending.

- **`3d89f8c`** — **Template Builder skills section restructured to match open-resume.** `skills: string` replaced with `TBSkills { featuredSkills: TBFeaturedSkill[]; descriptions: string }`. 6 named skill inputs each with 5-circle proficiency rating UI (filled = accent color). PDF and HTML preview render featured skills in a 3-column grid with filled/unfilled circles, category description lines below. Old string-format localStorage data auto-migrated on load. Store actions changed from `setSkills(string)` to `setFeaturedSkill(idx, skill, rating)` + `setSkillDescriptions(string)`.

- **`4efc528`** — **UMBC branding variant system.** AppShell now detects @umbc.edu users via email domain check in the auth useEffect. Logo components (LogoMark, LogoFull) accept optional `variant` prop ("resunova" | "umbc"); UMBC variant swaps mark color from amber (#c4793a) to UMBC gold (#b8860b) and wordmark from "Resunova" to "UMBC". Created UmbcProvider context to propagate isUmbc state across app. Added UmbcWelcomeBanner component (dismissible, session-local) that shows gradient banner + education messaging for UMBC users. Added `isUmbcUser()` utility in new `web/lib/userDomainDetection.ts`. Updated `web/lib/brand.ts` with `BrandVariant` type and `getBrandVariant()`. Added typography CSS variables in `web/app/globals.css` (--font-size-xs through --font-size-4xl, --font-weight-normal through --font-weight-extrabold, --line-height-tight through --line-height-relaxed) as foundation for future type-scale unification; variables are defined but not yet applied to all components (avoiding sed batch-replacement issues). Architectural decision: variant state lives in AppShell context + UmbcProvider, not URL/query params, so UI state persists per session without storage.

- **`69e26b2`** — Per-bullet Option A. After `_filter_bullet_rewrites` runs in `_normalize_analysis`, sweep `bulletAnalysis` and drop any bullet where (a) `improvedBullet` is empty, (b) `categoryRewrites` is empty, AND (c) `score >= 70`. A 75-score bullet with no AI help is dead-end UX — same pattern as the category-level fix in `d4f2641`. A genuinely weak bullet (<70) without a rewrite stays so the user at least sees the low score and the tags. Live test went from 2 → 1 entries on KrishResume-Analytics.pdf, with the silent 75-score "Reduced manual data-entry toil…" being the one dropped.
- **`93bf5e3`** — Added per-theme `--green-ink` / `--amber-ink` / `--red-ink` CSS variables for text-on-tint cases. Light mode green-ink is `#047857`; dark mode keeps `#34d399`. Strength chips at top of Analyze view + "✓ Fixed" badges on MatchBreakdownCards swapped to `--green-ink`. Light-mode contrast goes from ~2:1 → ~7:1 (WCAG AAA). Pattern documented inline so the next chip uses the right variable.
- **`91b08cc`** — Prod `/api/export-pdf-html` was 500-ing because the Railway Dockerfile only installed the `playwright` PyPI wrapper, not the Chromium browser binary nor its system deps. The handler also had no try/except, so the launch-failure exception escaped the CORS middleware and surfaced in the browser as a misleading CORS block. Fix: `RUN playwright install --with-deps chromium` in the Dockerfile (~300MB image bloat, acceptable), and a try/except around the executor that returns JSONResponse with three distinct error paths (ImportError → 503, "Executable doesn't exist" → 503 with install instruction, other → 500). All three go through CORS middleware so the browser sees real errors. Live verification: prod returned 200 + a real PDF on the 6th poll attempt (~2:35 after push) once Railway finished rebuilding.

- **`f2f7108`** — This file. Root `CLAUDE.md` added with architecture map, honesty-pipeline reference, component map, LLM tier matrix, 8 named invariants, and this running log. `web/AGENTS.md` updated to point at it. The maintenance rule (read at session start, update after every commit) lives at the top of CLAUDE.md.
- **`af79efc`** — Analyze "Download PDF" no longer goes through LaTeX. Captures `paperRef.current.outerHTML` and routes through `/api/export-pdf-html` (Chromium). Now byte-for-byte WYSIWYG with the preview. LaTeX path stays for tailor flow. Drive-by improvements to LaTeX path (section order pass-through, tech-stack `ProjectItem.tech` field, arrow encoding fix in `_latex_escape`) so the tailor flow's LaTeX output stops drifting from preview too.
- **`da08374`** — Added amber count badge to COMPLETED sidebar entries. A category like Achievement 82 with 1 weak bullet now shows `Achievement [1] 82` instead of looking identical to fully-clean categories. TOP FIXES badge stays red (existing). No badge = fully clean.
- **`d4f2641`** — Honest categorization in the sidebar. A category < 70 with no flagged bullets and no related topIssues now moves to COMPLETED instead of sitting in TOP FIXES as a dead-end. Both buckets now check `categoryHasActionableContent(key)`.
- **`004424b`** — Stopped opening the "AI Suggestion" popup when `improvedBullet === ""`. After the no-op-rewrite filter (`dc41ee5`) landed, the empty popup was firing on every honest no-op. Now those clicks route to the matching flagged-bullet card on the left via `onBulletLinkedSelect` instead.
- **`40d383d`** — Added 58 pure-Python dimension tests at `resume_gui/tests/test_analyze_dimensions.py`. Drive-by fix on `_NON_ISSUE_ATS_WARNING_RE` (trailing `\b` was blocking plurals — same bug as the earlier metric regex).
- **`c32f17e`** — Main analysis prompt now runs through `grok-4` reasoning tier by default (`ANALYSIS_MODEL` env). Score honesty jumped (74 → 84 on Krish's PDF). The validator stripping of `quantification` tags / no-op rewrites mostly doesn't have to fire anymore because grok-4 emits cleaner output at the source.
- **`dc41ee5`** — Drop no-op rewrites, strip lying `quantification` tags, drop non-issue atsWarnings. The three biggest "honesty drift" categories I'd been seeing.
- **`9556370`** — Cleanup of vision-doc artifacts that surfaced in the synthesized preview: doubled CGPA strings, `(Ongoing)` rendering as a literal bullet, `"Co · Tardeo, Mumbai | Tardeo, Mumbai"` duplication.
- **`cb2c4c5`** — Synthesizer + pipeline change: when vision extract succeeds, synthesize a clean text view from the structured doc and use it for BOTH the analysis prompt and the preview's `extractedText`. Score moved 62 → 72 because analysis stopped grading column-extraction artifacts.
- **`6a06619`** — **Vision-PDF is now the primary structured-extract path.** PyMuPDF renders PDF → PNG → Grok-4 vision call → structured JSON → `_vision_raw_to_resume_doc`. Faster (~11s vs ~25s) AND more accurate than the text-based path. Text path is now the fallback.
- **`8c4724d`** — Bullet-stitching post-processor (`_stitch_wrapped_bullets`) for the text-extract path. Multi-display-line wrapped bullets get joined back. Lives in both `resume_gui/app.py` and `linkedin_agent/resume_upload_parse.py` (the latter is the one MarkItDown's text actually flows through).
- **`b807bfb`** — Frontend `looksLikeSectionHeading` tightened. Rejects lines containing `[0-9%()/–—]` so `"ICSE — 97.16% (2021)"` stops being styled as a blue section heading.
- **`491a9db`** — Routed the structured-extract reasoning step to `grok-4-fast-reasoning` for cleaner JSON output. Added `model_override` parameter to `_llm_json_call`.
- **`d5273b1`** — The validator + calibration foundation (originally `44226d5` then lost during a Cursor force-push, recovered via reflog). The whole honesty pipeline starts here.

- **`4cf882a` (superseded) → `aa5d92f` + `fe2b0bf` (resunova-web staging)** — **Analyze box-wise inline editing, done right.** First attempt (`4cf882a`) enabled section editing by setting `presentationOnly={false}` on `AnalyzePreviewPane` — WRONG: that flag is overloaded (`AnnotatedResumePanel.tsx:729` sets `width: presentationOnly ? "100%" : 460`), so it collapsed the Analyze preview to the 460px Tailor-builder layout (empty space on the right) and swapped in the wrong toolbar. **Reverted, then rebuilt gated on `fieldsEditable` (already `true` for Analyze via `AnalyzePreviewPane`), never touching `presentationOnly`** — so the preview stays full-width WYSIWYG. Three capabilities, all session-only, all stripped from PDF/DOCX export: (1) **Section headings** — click to select, click again to edit (textarea); fixed a latent bug where a selected heading only toggled off and never entered edit mode; edited names render back after blur. (2) **Bullets** — `buildBlocksFromStructured` now stamps each bullet with a stable path (`exp.N.bullets.M`); *neutral* (non-flagged) bullets are `contentEditable` with commit-on-blur + `fieldOverrides`, mirroring the existing paragraph field-edit pattern. Flagged bullets keep their AI-suggestion popup/card (no click conflict). (3) **Reordering** — section blocks carry a stable `key` (incl. `extra.N`); `buildBlocksFromStructured` honors a `sectionOrderOverride`; inline up/down controls appear on heading hover. **Key invariant: section name-edits are keyed by section *key*, not block index, so they survive a reorder; selection (transient) is cleared on reorder.** Export safety: `cleanForExport` (useHtmlPdfExport) already strips `[data-field-path]` affordances; added `[data-section-idx]` neutralization. Discoverability via `.az-editable-field`/`.az-editable-section` hover outlines in globals.css. All `npx tsc --noEmit` clean. **NOT yet live-tested with a real résumé in-browser (file upload blocked in the local preview harness) — verify the interactive edit/reorder/export round-trip on staging.** Deep structural editing still routes to the Template Builder ("Edit in Builder"), per the light-inline strategy.

- **`c842500`** — **Profile page v2 activated with real backend data.** Replaced old form-heavy ProfilePage.tsx with 3-tab dashboard (Dashboard/Career Profile/Settings). Backend `/api/profile-dashboard` now runs real Supabase queries: scan counts from existing scan_limits service, recent activity merged from `resume_analyses`/`resumes`/`template_builder_resumes` tables, and total counts per user. Frontend `useProfileDashboard` hook switched from mock data to real API with error handling (401 for auth, 500 for server). Added missing shadcn/ui components (tabs, alert-dialog) + @radix-ui dependencies. Invariant: Profile requires authentication; 401 gates anonymous access. **TypeScript clean:** fixed parameter typing in ProfilePageClient and created alert-dialog.tsx / tabs.tsx UI components with proper Radix bindings.

- **Profile-aware jobs feed + onboarding chip UX (api `369b687`, web `48296af` + `e9b67fb` on staging)** — Backend `api_jobs_feed` now fetches `user_profiles` and injects `titleMatch`/`locationMatch` per posting; feed sorts by (role+location) > (role match) > unmatched, then `matchScore`. Frontend `JobsFeed.tsx`: "🎯 Your roles" filter pill + target-role badge on cards; inline chip-select nudge when `profileRoles` is empty (saves to profile + refreshes feed in place). `ProfilePage.tsx`: Job preferences Card replaced with 12 ROLE_SUGGESTIONS chip toggles backed by `form.roles` comma-string + text input fallback + "Powers your Jobs tab" header. Both "Manual wizard →" broken links (onboarding step-1 and sparse-profile banner) replaced with `<button onClick={finishOnboarding}>` — `/?view=manual-form` never existed. **Invariant:** profile lookup on feed is non-fatal — missing `user_profiles` falls back to pure `matchScore` sort. **Dev-tooling note:** SWC compile errors in dev can be caused by a stale `.next/cache`; clear it with `rm -rf .next/cache` and restart the dev server — tsc passing does NOT guarantee SWC won't choke on the same file.

---

*Last updated: 2026-06-16 (latest) — Jobs: show ALL requirements + warm "tangy" match UI (web `0cc99cd` on staging): dropped the job-detail 14-chip requirement cap (avg 18.3 reqs/posting, 64% >14) → required-gaps-first ordering + "Show all N" toggle + pre-scan teaser (no more empty "0/N MATCHED"); same cap removed in Boost Step 1; warm restyle = amber "+ add" pills / green ✓ matched / terracotta progress meter on detail + Boost + feed cards (warm color is brand `#c4793a`, NOT `--accent` which is link-blue here). Verified light+dark by stubbing the jobs API; tsc clean; NOT QA'd with real auth'd résumé. Landed via throwaway worktree off `origin/staging` because the shared `resunova-web` checkout was on `feature/tb-import-resume` (parallel-agent WIP) — my edits still linger uncommitted there. Earlier — Jobs ↔ Interview Prep link + "Prep for interview" widget (api `5ea1b0f`, web `56c0994` on staging): migration 018 adds `interview_prep_sessions.job_posting_id` (applied to staging); `GET /api/interview-prep/job-statuses` for the "Prep ready" badge; prep can be launched prefilled from a job card (JobsFeed `🎤 Prep interview` btn) or the job detail "Prepare for your interview" card. api 13/13 tests, tsc + webpack build clean, route live (401). **NOT visually QA'd — verify the jobs widgets + job→prep round-trip on staging (needs sign-in + real job data).** Done in throwaway worktrees off origin/staging (parallel agent now running tb-import/builder-ai/two-column feature worktrees in the shared checkouts). Earlier — Interview Story Bank made a real accumulating master bank (api `14ae984`, web `92e7897` on staging) to match santifer/career-ops: user-level accumulation (cross-session title-dedupe, cap 10), `GET /api/interview-prep/stories` + `DELETE /api/interview-prep/story/{id}`, "Your Story Bank" dashboard panel, migration 017 (stories.session_id FK CASCADE→SET NULL, applied to staging). api 10/10 tests; panel verified via prod build. **CONCURRENCY: both api+web are shared single checkouts a parallel agent was actively using (`git add -A` + branch switches) — my api work got bundled into its `14ae984` (mislabeled "tb-generate"); web commit landed on `feature/tb-import-resume`, recovered to staging via worktree cherry-pick. Never assume current branch in a shared checkout.** Deferred vs career-ops: per-question→story mapping w/ fit + gap detection; story editing. Earlier today — Interview Prep (STAR + story bank) merged to staging (api `02270d4`, web `79a6250`) + 3 migrations (`interview_prep_sessions`/`questions`/`stories`) applied to staging Supabase `uvnncrtulyezsylcxnhw`. Clean merges (api: 1 import-block conflict; web: zero). Fixed 2 bugs: web generate-call missing `Authorization` header (persistence never ran) + api stale test patch targets. QA: api 6/6 + generate E2E + staging-DB CRUD round-trip + web tsc; UI verified via prod build + static-serve (dev `next dev` can't render dedicated `app/*` routes — Suspense hangs, template-builder reproduces; QA those via `next build`). **Landmine: local backend `SUPABASE_URL`=prod `eiumlptnsmowvkxucprl` while web `.env.local`=staging — local persistence E2E impossible, don't write prod.** PROD untouched (prod Supabase lacks the 3 tables). Earlier: Cover Letter Builder responsive layout fix on staging (web `3e1fee5`): header wrap + ResizeObserver-driven panel stacking <880px + scale-to-fit preview; PDF export invariant preserved. Earlier same day: Cover Letter Builder merged to staging (api `dd8c5f8`, web `990ddf9`) + `012_cover_letters.sql` applied to staging Supabase `uvnncrtulyezsylcxnhw`. Web branch was 41 commits behind → rebased + 2 divergence type-fixes; merged via throwaway worktree; typechecked, NOT browser-verified. Migration filename number `012` collides with web's existing `012_job_postings.sql` — renumber to `021` before reconciling. Earlier: Tabbed Profile shipped to `resunova-web` staging (`6cdbb8e`) — existing page regrouped into Dashboard/Career/Settings tabs, all logic preserved. Earlier: email marketing (Brevo + Resend) on staging (api `cf1e3e0`, web `155dca9`); staging Supabase migrated + staging AND prod Railway env vars set. PROD still needs: `notify_prefs` column on prod Supabase `eiumlptnsmowvkxucprl` (no MCP access — owner must run the ALTER), and the web prod release (staging→main = 32-commit release w/ unapplied prod DB migrations 013–019). **Repo-topology note in the email-marketing changelog entry is essential — deploys come from resunova-api/resunova-web, NOT this repo.** Next session: read this file first, update it last.*
