# Core LangGraph and LangChain dependencies
langgraph>=0.3.27
langgraph-sdk>=0.1.66
langgraph-checkpoint>=2.0.23
langgraph-cli[inmem]>=0.4.0
langchain>=0.3.0
langchain-core>=0.2.38
langchain-anthropic>=0.1.0

# LLM Providers (add others as needed)
langchain-openai>=0.2.0
langchain-google-genai>=1.0.0
langchain-xai>=0.2.0

# Tools and utilities
langsmith>=0.1.63
python-dotenv>=1.0.0

# LinkedIn Scraping - Choose your method
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=4.9.0

# LinkedIn API - For authenticated profile and job access
linkedin-api>=2.3.1

# Optional: More robust scraping (Method 2)
# Uncomment if you want to use linkedin-jobs-scraper
# linkedin-jobs-scraper>=1.15.0
# selenium>=4.15.0

# Browser automation for Easy Apply — required for apply_to_job
# After installing, run: playwright install chromium
playwright>=1.40.0

# HTTP and async
httpx>=0.25.0
aiohttp>=3.9.0

# Data processing
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Optional: Database for persistence
# pymongo>=4.6.0
# psycopg2-binary>=2.9.9

# Optional: Vector stores for RAG (future enhancement)
# chromadb>=0.4.0
# faiss-cpu>=1.7.4

# PDF text extraction (for resume upload feature)
pdfplumber>=0.11.0
python-multipart>=0.0.9

# Supabase Storage — persists generated PDFs/.tex across Railway redeploys
supabase>=2.5.0

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0